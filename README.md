# Cargo Motives

B2B container logistics marketplace for Tanzania — verified transporter companies bid on shipper jobs, with live GPS tracking where available and a 30% platform commission collected via mobile money.

Full specification lives in the sibling `Cargo Motives v4 DOCs` folder (PRD, TRD, AppFlow, UI/UX Brief, Backend Schema, Implementation Plan). This repo implements that spec phase by phase; this README tracks vendor decisions and current build status so either of us can pick the thread back up without re-deriving context.

## Repo layout

```
backend/             Laravel — REST API, Admin (Livewire), Driver Link pages
mobile/               Flutter — Customer + Transporter Company app (Android + iOS)
docker/php/           Dockerfile for the backend's PHP containers (see "Docker" note below)
docker-compose.yml     Postgres+PostGIS, Redis, MinIO, the Laravel app, queue worker, Reverb
.local-services/redis/ Portable Redis-for-Windows build used for local dev on this machine (gitignored — see setup below)
```

## Vendor / technology decisions

| Decision | Choice | Status |
|---|---|---|
| Backend | Laravel 13, PHP 8.3+ | scaffolded |
| Database | PostgreSQL + PostGIS | **running** — installed natively (v18.4 + PostGIS 3.6.2 on this machine; the Docker image targets v16, either works, nothing in the schema is version-specific) |
| Cache/Queue/Broadcast | Redis (via `predis`, not the phpredis extension — works identically whether or not that PHP extension is installed) | **running** — see "Local development setup" |
| Real-time | Laravel Reverb (WebSockets) — used only for live GPS + live bids per TRD §4 | installed |
| Object storage | S3-compatible; MinIO locally, a real bucket in staging/prod | `App\Services\Documents\DocumentStorage` — **working**, but currently backed by the local disk (Docker/MinIO isn't running on this machine — see below); disk-agnostic by design, so switching to S3/MinIO later is a config change, not a code change |
| Auth | Laravel Sanctum (Customer/Company/Admin); a separate short-lived signed token for the Driver Link | installed |
| Mobile | Flutter, one app, role-aware routing (`go_router`) | scaffolded |
| Maps | **Google Maps** | key not yet provisioned — set `GOOGLE_MAPS_API_KEY` |
| Mobile money | **Selcom** | building against sandbox; real merchant credentials needed before go-live |
| First GPS provider | **Wialon** (Phase 6) | not started |
| SMS gateway | **Deferred** — `SMS_DRIVER=log` (writes to the log instead of sending) until a provider is chosen. Leading candidate: Beem Africa. | driver abstraction built (`App\Services\Sms`), no real provider wired yet |
| Error monitoring | Sentry (planned, TRD §10) | not yet added |
| Auth | Phone + OTP, shared by Customer & Transporter Company (PRD §6) | **working** — Sanctum tokens, Redis-backed OTP, rate-limited |
| Admin login | Email + password (`users.password_hash`) | **working** — minimal, ahead of the full Admin tool (Phase 9); bootstrap the first admin with `php artisan admin:create <phone> <email> <password>` |

## Local development setup

**Docker isn't usable on this particular machine** — it's physical hardware without VT-x/AMD-V exposed to Windows, so WSL2 (and therefore Docker Desktop) can't start a VM. `docker-compose.yml` is still there and correct for any machine that *does* support virtualization (a teammate's machine, CI, staging), but local dev here runs Postgres and Redis natively instead. If you're on a machine where Docker works, skip to "Alternative: Docker" below.

### 1. PostgreSQL + PostGIS

Already installed on this machine (PostgreSQL 18.4, via the official EDB installer, with the matching [PostGIS Windows bundle](https://postgis.net/windows_downloads/) installed on top). On a fresh machine:

1. Download PostgreSQL from https://www.postgresql.org/download/windows/ and install it (note the superuser password you set).
2. Download the matching PostGIS bundle for your PostgreSQL major version from https://download.osgeo.org/postgis/windows/ (e.g. `pg18` → `postgis-bundle-pg18x64-setup-*.exe`) and run it.
3. Create the app's role, database, and enable the extension (run as the `postgres` superuser):
   ```sql
   CREATE ROLE cargo_motives LOGIN PASSWORD 'secret';
   CREATE DATABASE cargo_motives OWNER cargo_motives;
   \c cargo_motives
   CREATE EXTENSION IF NOT EXISTS postgis;
   ```
   This matches `backend/.env`'s `DB_USERNAME`/`DB_PASSWORD` already — no `.env` changes needed. The superuser password is never stored in the app config, only used for this one-time setup.

**Note on installers needing elevation:** on this machine, running the Postgres/PostGIS installers (and an MSI-based Redis installer we tried first) from an automated/non-interactive shell failed — Windows couldn't show the UAC consent prompt those installers need. Running them from a normal interactive **Administrator** PowerShell/terminal works fine.

### 2. Redis (portable, no installer)

An MSI-based Redis-compatible service (Memurai) consistently failed to install on this machine even from an elevated terminal — Windows Installer's own SYSTEM-level temp directory was inaccessible, which looks like a machine-specific restriction unrelated to normal admin rights. The workaround: a portable, dependency-free Redis build ([tporadowski/redis](https://github.com/tporadowski/redis/releases), an unofficial but widely-used Windows port) that's just an `.exe` — no installer, no service, no elevation needed.

Already set up in `.local-services/redis/` (gitignored — it's a downloaded third-party binary, not project source). To start it:

```powershell
powershell -File .local-services\start-redis.ps1
```

This is **not a Windows service** — it's a plain foreground process, so it needs to be started again after every reboot or terminal close. Leave that terminal open while working on the backend, or run it in its own background terminal/tab.

### 3. Backend

```bash
cd backend
php artisan migrate
php artisan serve
```

### 4. Verify

```bash
curl http://localhost:8000/api/health
```

Should return `{"status":"ok","checks":{"database":{"ok":true},"redis":{"ok":true}}}`. If either check is `false`, the error message names which dependency is unreachable and why — see `app/Http/Controllers/HealthController.php` for what each failure mode means.

### Alternative: Docker (on a machine that supports virtualization)

```bash
cp .env.example .env
docker compose up -d
docker compose exec app php artisan migrate
curl http://localhost:8000/api/health
```

This also brings up MinIO (S3-compatible storage) and Reverb, which the native setup above doesn't include yet — needed once file uploads (Phase 2+) or WebSockets (Phase 4+) are being tested locally, if this machine's Docker situation doesn't change before then.

### Mobile app

```bash
cd mobile
flutter pub get
flutter run          # or: flutter run -d chrome
```

## Phase status

Tracking the Implementation Plan document's 12 phases. Each phase is built, explained, and verified before the next starts.

- [x] **Phase 0 — Foundations**: Laravel + Flutter scaffolds, Docker Compose, vendor choices documented, `/api/health` endpoint, SMS driver abstraction. Verified end-to-end against a real (natively-installed) Postgres+PostGIS and Redis — not just against sqlite/mocks.
- [x] **Phase 1 — Auth (Customer & Company)**: shared phone/OTP flow (`App\Services\Auth\OtpService`, Redis-backed with attempt lockout + resend cooldown), phone number normalization, Sanctum tokens, rate limiting (`otp-request`/`otp-verify` limiters), Customer profile completion. Flutter: Phone Entry → OTP → Profile Setup screens, `ApiClient`/`AuthRepository`, session persistence. 29 backend tests + 13 Flutter tests passing; walked the full flow live in a browser against the real stack for both roles (Customer → Profile Setup → Customer Home; Transporter Company → Company Home placeholder). Found and fixed two real bugs along the way: a default Laravel guest-redirect that 500'd unauthenticated API requests lacking an `Accept: application/json` header, and an OTP lockout off-by-one.
- [x] **Phase 2 — Company Verification (manual review, anti-duplicate)**: two-section verification submission (`App\Http\Controllers\Company\CompanyVerificationController`), `CompanyDuplicateDetector` (a registration number/TIN/NIDA collision is flagged for Admin, never silently accepted or rejected — deliberately *not* a DB-level unique constraint, since that would make flagging impossible), private document storage with signed URLs (`DocumentStorage`), and a minimal Admin login + review API (list/approve/reject) ahead of the full Admin tool (Phase 9). Flutter: `CompanyHomeGate` routes a Transporter Company session to the verification form, a pending-review screen, or Company Home based on live status — checked fresh on every session, not just after login. 61 backend tests + 22 Flutter tests passing; walked all four states (submit → flagged-duplicate → admin reject → resubmit → admin approve → Company Home) live against the real stack, including a live approve/reject through the Admin API. Found and fixed three real bugs: two mass-assignment gaps where a field silently got dropped because it wasn't in a model's `$fillable` list (`owner_user_id`, then `password_hash` — the latter meant the admin-bootstrap command created unusable accounts, invisible to factory-based tests since factories bypass mass-assignment), and a `setState()` given a closure that returned a `Future` (an easy-to-miss Dart gotcha from an arrow function whose body was an assignment expression).
- [x] **Phase 3 — Trucks & Drivers**: truck registration with independent Admin verification (`App\Http\Controllers\Company\TruckController` — same create-or-resubmit-if-rejected shape as company verification), a driver roster with no verification workflow (drivers are never Admin-reviewed, PRD §5), a reusable `EnsureCompanyApproved` middleware (fleet management only opens up once the company itself is approved — Phase 4's bidding will need this exact check again), and a matching Admin truck-review API. Flutter: a real Company Home shell (bottom nav — Jobs/Fleet/Earnings/Profile, UI/UX Brief §3) with a working Fleet tab (Trucks/Drivers sub-tabs, add/resubmit forms with `file_picker`) and a minimal but real Profile tab (company name + logout). 83 backend tests + 37 Flutter tests passing; walked the full flow live — register two trucks, approve one/reject the other via the Admin API, add a driver, confirm the Fleet tab and Profile tab both render real data end-to-end in a browser.
  - Found and fixed a real bug affecting every phase's models so far, not just this one: Eloquent's `create()` doesn't reflect a migration's DB-level `->default(...)` values on the in-memory model it returns — a freshly-registered truck's `gps_status`/`current_status`/`is_active` were `null` in the live API response despite the DB row being correct. Fixed by declaring `protected $attributes` on `User`, `TransporterCompany`, `Truck`, and `Driver` to mirror their migrations' defaults.
  - Found and fixed a real Flutter/Dio bug before it ever shipped: Dio's default `ListFormat.multi` only brackets a list entry when the entry is a Map/List, not a `MultipartFile` — so uploading multiple truck photos would have silently sent them all under the same bare `photos` field name, and Laravel/PHP keeps only the *last* of several identically-named non-bracketed multipart parts. Fixed with `ListFormat.multiCompatible`; pinned with a regression test since it's the kind of thing a future Dio upgrade or an "innocent" refactor could silently reintroduce.
- [x] **Phase 4 — Job Posting & Bidding**: jobs/bids schema (Backend Schema §2.7) with PostGIS `geography(Point,4326)` pickup/dropoff columns (no Eloquent cast exists for these — written via raw `ST_SetSRID(ST_MakePoint(...))` through a small `GeoPoint` value object, read back via a `withCoordinates()` query scope using `ST_X`/`ST_Y`), a partial unique Postgres index enforcing "one pending bid per company per job" (`WHERE status = 'pending'`, not a plain unique constraint — a company can re-bid after withdrawing or being rejected), and Redis sorted-set rolling-window quotas (`RollingQuotaService`, `BidQuotaService`, `JobPostQuotaService`) for both customer job-posting (5/day standard, 10/day Featured) and company bidding (5/24h standard, 10/15h Featured) — deliberately not fixed-window `INCR`+`EXPIRE`, which lets a burst empty and refill a quota right at the window boundary. Bid acceptance (`BidController::accept`) runs in a DB transaction with `lockForUpdate()`, assigning the job and rejecting every other pending bid atomically. First real use of Reverb: `BidPlaced` broadcasts on a private `job.{jobId}` channel, authorized by a class-based `App\Broadcasting\JobChannel` (not an inline closure) so channel authorization is directly unit-testable without an HTTP round trip. Flutter: Customer Post-a-Job form + Job Detail screen with a live-updating bid list (Featured bids pinned first) via a hand-rolled minimal Pusher-protocol WebSocket client (`JobBidChannel`, using `web_socket_channel` — the obvious `pusher_channels_flutter` package turned out to hardcode Pusher's own cloud infrastructure host with no way to point it at a self-hosted Reverb server) and Accept Bid; Company Open/My Bids/Active job feeds (tabbed, mirroring the Fleet tab's Trucks/Drivers split) and a bid-placement form with live quota display. 132 backend tests + 55 Flutter tests passing. Walked the full flow live against the real stack (Postgres, Redis, a running Reverb server, and a queue worker — `BidPlaced` is a *queued* broadcast, so nothing reaches a socket without one): posted a job as a customer, subscribed to its private channel with a real WebSocket client, placed a bid as an approved company, and watched the `bid.placed` event arrive over the open socket in real time before accepting the bid and confirming the job transitioned to `assigned` with `agreed_price` set; also confirmed quota counters decrement correctly and a duplicate-pending-bid attempt is rejected with 422.
  - Found and fixed a real test-infrastructure gap, not just a Phase 4 edge case: PostGIS raw SQL (`::geography`, `ST_MakePoint`) cannot run against SQLite at all, and PostGIS is now core to the schema — so `phpunit.xml` was switched from an in-memory SQLite DB to a dedicated real Postgres database (`cargo_motives_test`), with a dedicated Redis logical DB (`REDIS_DB=2`) alongside it so quota tests can't leak state into (or read stale state from) dev Redis.
  - Found and fixed a subtle Redis bug during that same isolation work: `Redis::keys('prefix:*')` returns keys *with* Laravel's configured key prefix already applied, so passing those results straight to `Redis::del()` applies the prefix a second time and silently deletes nothing — quota state was leaking between test runs (and into dev Redis) until this was caught via `tinker`.
  - Found and fixed a broadcast-channel-authorization testability dead end: `NullBroadcaster::auth()` (active under `BROADCAST_CONNECTION=null` in tests) never invokes registered channel-authorization callbacks at all, and switching a test to the real Reverb driver hit deeper signing complexity. Refactored the inline channel closure into `App\Broadcasting\JobChannel` so its `join()` logic could be unit-tested directly, sidestepping the broadcaster entirely.
- [x] **Phase 5 — Job Assignment, Driver Link & Proof of Delivery**: `driver_links` (a single-use, no-login, short-lived token scoped to one job — TRD §7: "never a Sanctum token") and `proof_of_deliveries` tables (Backend Schema §2.5/§2.10). `JobAssignmentService` picks a truck + driver for a job the company has already won (separate from Phase 4's bid acceptance — a finer-grained, re-doable step: a truck breakdown mid-job can be reassigned), transactionally frees the previous truck, flips the new one to `on_job`, invalidates any still-active link for the job, and issues a fresh one, texting it via the existing `SmsGateway` abstraction (still `SMS_DRIVER=log`) — a send failure degrades silently (TRD §5.3), since the link is always returned to the caller to display/re-share regardless. The Driver Link itself is the app's first server-rendered page (`resources/views/driver-link/*`, plain Blade + inline CSS, no Vite/Tailwind build step) rather than a Flutter screen — the TRD specifies it as a "lightweight server-rendered mobile web page, no login, no app install," and a driver has no account for a Flutter screen to authenticate. It lets a driver optionally advance the job through `en_route_pickup`/`picked_up`/`in_transit` (forward-only, enforced against the canonical status order) before submitting proof of delivery (camera-first `<input capture="environment">`, photos + optional recipient name/notes) via `DocumentStorage`, atomically marking the job `delivered` and the link `used`. "Fans out instantly to the Customer, the Company, and Admin's record" (AppFlow §4) is satisfied by that data becoming visible on the next fetch of the job (`JobResource` now exposes `assigned_truck_registration`/`assigned_driver_name`/`proof_of_delivery`) — the same ordinary REST + refresh-on-open pattern already used for non-bid/non-GPS features (TRD §4), not a new push channel; no `notifications`/FCM infrastructure exists yet in this codebase; that's explicitly out of scope here (Backend Schema's `notifications` table isn't a deliverable of any of the 12 phases, and `activity_logs` writes are explicitly Phase 9's job). The customer's Confirm Receipt (`JobController::confirmDelivery`) moves the job to `completed` and frees the truck back to `idle`; it deliberately does **not** write a `commission_ledger` charge (that table doesn't exist until Phase 7) or open a dispute for "Report a Problem" (Phase 9's Admin tool owns disputes) — both explicitly out of this phase's scope. Flutter: Company `AssignJobScreen` (idle/approved truck + active driver pickers, a copyable driver-link confirmation), an Assignment section on `CompanyJobDetailScreen` (gated on a new `is_assigned_to_viewer` flag — a company can legitimately view a job it *lost* the bid on via "My Bids" after it moves past `open`, and must not see actionable assignment buttons on someone else's job); Customer `JobDetailScreen` gained a Proof of Delivery card (photo thumbnails, recipient/notes) with Confirm Receipt. 153 backend tests + 62 Flutter tests passing, Pint clean, `flutter analyze` clean. Verified live end to end against the real stack: assigned a truck+driver via the API (SMS logged, truck flipped to `on_job`), opened the real Driver Link page in a browser and advanced its status live, submitted proof of delivery with a real JPEG via a raw HTTP multipart request (CSRF token + session cookie fetched from the page first) — job flipped to `delivered`, link to `used`, photo downloadable via its signed URL — then confirmed delivery as the customer via the API, landing the job on `completed` with the truck freed back to `idle`.
  - Found and fixed a real mass-assignment gap — the same bug class as Phase 2/3, still recurring: `DriverLink`'s `#[Fillable(...)]` omitted `used_at`, so `$link->update(['status' => 'used', 'used_at' => now()])` silently dropped the timestamp; a test asserting `used_at` was set (not just `status`) caught it immediately.
  - Found and fixed a `nextDriverSettableStatus()` logic bug before it shipped: `array_search($currentStatus, $list, true) + 1` looks reasonable until `$currentStatus` isn't in `$list` at all (the job's real starting status, `'assigned'`) — PHP silently coerces `array_search`'s `false` return to `0` under arithmetic, so `false + 1` evaluates to `1`, which would have skipped straight to "picked_up" as a driver's very first status option instead of "en_route_pickup". Caught during self-review before running any tooling; fixed by handling the `'assigned'` starting case explicitly rather than relying on `false`'s arithmetic behavior.
  - The Blade view for an expired/used link (`show()`) and the corresponding `abort_unless(..., 410)` in the status-update/POD-submit actions render differently — the former shows a friendly branded page, the latter Laravel's generic error page for the same underlying condition. Left as a known, minor UX inconsistency (the request is still correctly rejected either way) rather than a blocking bug, given the phase's time budget; worth a custom `resources/views/errors/410.blade.php` in a later polish pass.
- [x] **Phase 6 — GPS: Wialon, end to end**: `gps_connections` (encrypted per-company provider credentials, Backend Schema §2.6) and `job_location_snapshots` (throttled route-replay points, §2.9) tables; `trucks.gps_connection_id` finally gets its real FK now that the table exists. A concrete `WialonGpsProvider` (`App\Services\Gps`, bound directly to a `GpsProvider` interface — no multi-provider manager/framework yet, matching the plan's "prove one provider end to end before touching a second") implements Wialon's real RPC-style API (`token/login` → session id → `core/search_items` with `flags=1025` to get unit list + last-known position in one call), each call a fresh stateless login/query/logout round trip rather than a persisted session. Connect GPS (`GpsConnectionController`) authorizes a company's token, returns every visible unit with a plate-text-matched suggested truck, and `import()` links confirmed matches (`gps_connection_id`/`gps_unit_id`/`gps_status='connected'` on the truck) — an unmatched or skipped vehicle stays "GPS Tracking Not Available," never an error. The async pipeline (TRD §5.2) is poll-based, not webhook — Wialon has no outbound webhook in this integration, so a scheduled `PollGpsPositionsJob` (`bootstrap/app.php`'s `withSchedule`, every minute) fetches each connected company's units and dispatches a `NormalizeGpsPositionJob` per truck with a position; the normalizer updates `trucks.last_known_*`, recovers `gps_status` from `signal_lost`, throttle-writes a `job_location_snapshots` row, and broadcasts `TruckLocationUpdated` on a **second**, new private channel (`job.{jobId}.location` — distinct from Phase 4's `job.{jobId}` bid channel, since GPS's audience is broader: both the customer *and* the assigned company watch the same truck, authorized by a new class-based `JobLocationChannel`). A company/truck failing to poll (bad token, provider outage) is marked `error` and skipped — never crashes the cycle for other companies (TRD §5.3). A separate scheduled `gps:check-signal-loss` command flips a trackable job's `gps_signal_status` from `ok` to `lost` once its truck's last position is older than a configurable threshold; recovery back to `ok` happens automatically the instant a fresh position arrives. `JobAssignmentService` decides a job's GPS-trackability once, at assignment time, from the truck's own `gps_status` — not re-derived on every read. Flutter: a `ConnectGpsScreen` (Wialon-only picker — Traccar/Tracksolid Pro shown but disabled, "coming soon") for the authorize → match-units → import flow, wired into `FleetScreen` as an app-bar action; a shared `GpsStatusCard` on both `JobDetailScreen` and `CompanyJobDetailScreen` rendering the three documented states (not available / live / signal lost) kept current by a second hand-rolled WebSocket client (`JobLocationChannel`, mirroring `JobBidChannel`'s pattern exactly). No real map is rendered — the same documented, already-established scope decision as Phase 4's plain lat/lng job-posting fields: no Google Maps API key is provisioned, so this is an honest live position readout (coordinates, compass heading, "updated Xs ago") rather than map tiles, while still fully exercising the real WebSocket data path. 183 backend tests + 70 Flutter tests passing, Pint clean, `flutter analyze` clean. Verified live end to end against the real stack: assigned a GPS-connected truck via the API (`gps_tracking_active`/`gps_signal_status` set correctly), dispatched a real position update through the actual Redis queue and watched `location.updated` arrive over a live WebSocket subscribed as the customer, confirmed the REST job endpoint surfaces the same live coordinates, backdated a truck's position and ran the signal-loss command live (`ok` → `lost`), confirmed automatic recovery (`lost` → `ok`) on the next position update, and confirmed Connect GPS's real HTTPS call to Wialon's actual API degrades gracefully (422 with a clear message) when the account doesn't exist/network fails, and that `PollGpsPositionsJob` marks a real failing connection `error` without affecting others.
  - Found and fixed a real, would-have-shipped bug: Carbon 3 changed `diffInMinutes()`/`diffInX()` to return a **signed** difference by default (Carbon 2 always returned positive) — `$recordedAt->diffInMinutes($lastSnapshot->recorded_at)` with `$recordedAt` always the newer timestamp came back negative, so `< $throttleMinutes` was true unconditionally and silently disabled the snapshot throttle forever after the very first write. This behavior's own test caught it (expected 2 snapshots after a 6-minute gap, got 1) — inspection alone would not have; fixed with `abs()` and a comment warning against trusting a `diffInX()` sign in this codebase again.
  - Found and fixed a second, related bug in the same method: comparing a fresh `Carbon` instance (from an Eloquent model read) against a raw `->value('recorded_at')` query-builder fetch (an uncast DB-driver string) silently broke the same throttle comparison — fixed by fetching through the model (`->first()`) so casts apply.
  - Found (but didn't chase further) a real local-machine constraint worth remembering for Phase 7's Selcom integration: this machine's PHP/cURL cannot verify `hst-api.wialon.com`'s TLS certificate chain (`SSL certificate ... self-signed certificate in certificate chain`) — a genuine outbound-HTTPS environment quirk, not a code bug. `WialonGpsProvider`'s exception handling converted it into a clean, correctly-surfaced `GpsProviderException` → 422 either way, so Connect GPS degrades exactly as designed even against this machine's real network conditions; any future phase making its own outbound HTTPS call (Selcom, Firebase) should expect to hit the same local CA-trust issue and not mistake it for a bug in that integration's code.
- [x] **Phase 7 — Commission Ledger & Mobile Money (Selcom)**: `payments` and `commission_ledger` tables (Backend Schema §2.13/§2.14 — `commission_ledger` uses the schema's literal singular table name, not Eloquent's pluralized default, so the model sets `$table` explicitly; append-only, `const UPDATED_AT = null`). A single `CommissionLedgerService` is the *only* place that ever writes a ledger row or mutates `transporter_companies.outstanding_balance`/`commission_standing` — `chargeForCompletedJob()` (fired from `JobController::confirmDelivery`) adds `agreed_price × commission_rate_default%` (a `platform_settings` value, seeded to 30 to match the TRD's fixed "30%," but Admin-adjustable later, not hardcoded) as a charge; `applyPayment()` (fired only from a confirmed webhook, never the initiate-payment response) subtracts a succeeded payment, clamped at zero. Both lock the company row first (`lockForUpdate`, the same pattern as Phase 4's bid acceptance) so a charge and a payment landing at nearly the same moment can't interleave into a wrong balance, and both recompute `commission_standing` fresh from the resulting balance against `commission_hold_threshold` (seeded to TZS 500,000 — no source number exists in the docs, picked as a working placeholder since the phase's own deliverable needs one) in either direction, so it can never drift. `BidController`'s on-hold bid-rejection (stubbed back in Phase 4) now has real balance changes to react to. A concrete `SelcomMobileMoneyGateway` (bound directly to a `MobileMoneyGateway` interface, same one-aggregator-no-framework reasoning as Phase 6's GPS provider) pushes a USSD-style mobile money charge and verifies inbound webhook signatures (HMAC-SHA256 over the raw request body) — its docblock is explicit that, unlike Wialon's well-established public API, its exact wire shape (endpoint path, signed-header names) is a best-effort model with no live Selcom account to verify it against (`SELCOM_*` env vars are unset), while the domain logic it feeds (ledger, idempotency, graceful degradation) is fully built and tested independent of that uncertainty. `SelcomWebhookController` is unauthenticated (no Sanctum guard — Selcom's server has no session) but never trusted without a valid signature, and is idempotent both ways: a retried `COMPLETED` delivery never double-credits, and a late/out-of-order `FAILED` delivery never downgrades an already-`succeeded` payment. Flutter: a real `EarningsScreen` (replacing Phase 3's placeholder) with the running balance, a Pay via Mobile Money bottom sheet (amount capped client-and-server-side at the current balance), and the ledger history; an On Hold banner shown across every Company Home tab (not just Earnings) when `commission_standing` is `on_hold`, fetched once at shell level per AppFlow §2.1's "status banners only when relevant." 206 backend tests + 76 Flutter tests passing, Pint clean, `flutter analyze` clean. Verified live end to end against the real stack: completed a real job and watched the exact 30% charge land in the ledger and balance, completed a second high-value job that crossed the threshold and confirmed bidding was actually rejected (422) for that company, confirmed a real HTTPS charge-initiation call to Selcom's sandbox degrades gracefully (a real connection-timeout failure recorded as a clean `failed` Payment row, not a crash), then self-signed a webhook payload and confirmed it credits the ledger, lifts the hold, is correctly rejected with a wrong signature, and does not double-credit on a retried delivery.
  - Found and fixed a real bug before it ever ran: `CommissionLedger`'s table is named `commission_ledger` (singular, matching the schema doc literally) but Eloquent's naming convention guesses `commission_ledgers` (pluralized) from the class name — the very first test against it failed with `relation "commission_ledgers" does not exist`. Fixed with an explicit `protected $table = 'commission_ledger';`. Every other new model this phase (and Phase 4-6's) happened to pluralize regularly and never hit this; worth checking explicitly whenever a schema-doc table name doesn't sound like a clean English plural.
  - Confirmed Phase 6's local-machine outbound-HTTPS note applies here too, just differently: the live charge-initiation call to Selcom's sandbox failed with a connection timeout (not the TLS chain error Wialon hit) — a different failure mode, same underlying "this machine's real network path to a third-party HTTPS API is unreliable" constraint, and the same graceful-degradation code handled it correctly either way.
- [ ] Phase 8 — Featured Tier & Messaging
- [ ] Phase 8 — Featured Tier & Messaging
- [ ] Phase 9 — Admin Tool
- [ ] Phase 10 — Hardening & Launch Prep
- [ ] Phase 11 — Launch & Learn
