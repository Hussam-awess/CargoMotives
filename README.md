# Cargo Motives

B2B container logistics marketplace for Tanzania — verified transporter companies bid on shipper jobs, with live GPS tracking where available. Customers pay transporters directly, outside the app; the platform's revenue comes entirely from Customer Plus and Transporter Plus subscriptions (Phase 10.13 — earlier phases built a 30% commission model, since removed).

Full specification lives in the sibling `Cargo Motives v4 DOCs` folder (PRD, TRD, AppFlow, UI/UX Brief, Backend Schema, Implementation Plan). This repo implements that spec phase by phase; this README tracks vendor decisions and current build status so either of us can pick the thread back up without re-deriving context.

## Repo layout

```
backend/             Laravel — REST API, Admin (Livewire), Driver Link pages
mobile/               Flutter — Customer + Transporter Company app (Android + iOS)
docker/php/           Dockerfile for the backend's PHP containers (see "Docker" note below)
docker-compose.yml     Postgres+PostGIS, Redis, MinIO, the Laravel app, queue worker, Reverb
.local-services/redis/ Portable Redis-for-Windows build used for local dev on this machine (gitignored — see setup below)
```

## Vendor / technology decisions

| Decision | Choice | Status |
|---|---|---|
| Backend | Laravel 13, PHP 8.3+ | scaffolded |
| Database | PostgreSQL + PostGIS | **running** — installed natively (v18.4 + PostGIS 3.6.2 on this machine; the Docker image targets v16, either works, nothing in the schema is version-specific) |
| Cache/Queue/Broadcast | Redis (via `predis`, not the phpredis extension — works identically whether or not that PHP extension is installed) | **running** — see "Local development setup" |
| Real-time | Laravel Reverb (WebSockets) — used only for live GPS + live bids per TRD §4 | installed |
| Object storage | S3-compatible; MinIO locally, a real bucket in staging/prod | `App\Services\Documents\DocumentStorage` — **working**, but currently backed by the local disk (Docker/MinIO isn't running on this machine — see below); disk-agnostic by design, so switching to S3/MinIO later is a config change, not a code change |
| Auth | Laravel Sanctum (Customer/Company/Admin); a separate short-lived signed token for the Driver Link | installed |
| Mobile | Flutter, one app, role-aware routing (`go_router`) | scaffolded |
| Maps | **Google Maps** | key not yet provisioned — set `GOOGLE_MAPS_API_KEY` |
| Mobile money | **Selcom** | building against sandbox; real merchant credentials needed before go-live |
| First GPS provider | **Wialon** (Phase 6) | not started |
| Push notifications | **Firebase Cloud Messaging**, project `cargo-motives` (Phase 10.6, real credentials Phase 10.6 follow-up) | **real, live-verified** — `PUSH_DRIVER=firebase` in `backend/.env`, service-account key at `backend/storage/app/firebase/` (gitignored), `google-services.json`/`GoogleService-Info.plist` in the mobile Android/iOS projects; confirmed a genuine OAuth2 handshake with Google by sending to a fake token and getting Google's own rejection back. Android app builds successfully with it (`flutter build apk --debug`); **not yet watched arriving on an actual device or emulator** — this machine couldn't boot its one configured AVD (disk space), so that specific last-mile check is still open whenever a device is available |
| SMS gateway | **Beem Africa** (Phase 10.12) — `App\Services\Sms\Drivers\BeemSmsDriver`, real HTTP calls against Beem's v1 API. Only needed for the Transporter Company phone+OTP flow and the Driver Link text (Phase 10.5 moved Customer off SMS entirely). `SMS_DRIVER` still defaults to `log` until real `BEEM_API_KEY`/`BEEM_SECRET_KEY` are set — no live Beem account exists in this environment yet, so the exact wire format is modeled on Beem's public docs, not verified end-to-end (same caveat as Selcom). | **driver code complete, live-verifiable once real credentials exist** — set `SMS_DRIVER=beem` + `BEEM_API_KEY`/`BEEM_SECRET_KEY`/`SMS_SENDER_ID` (an ID Beem has actually approved, ≤11 chars) in `backend/.env` to go live; both OTP (`OtpService`) and the Driver Link (`JobAssignmentService`) already send through the `SmsGateway` interface with zero call-site changes needed |
| Mail gateway | **Gmail SMTP** (`smtp.gmail.com:587`, an app-password-authenticated account) for the Customer email-OTP flow added in Phase 10.5. Fine for MVP/low-volume; a dedicated transactional provider (Postmark/SES/etc.) is the natural next step if volume or deliverability becomes a concern. | **working** — live-verified with a real end-to-end send (Gmail delivered a real code, verified, account created); `phpunit.xml` forces `MAIL_MAILER=array` so the test suite never sends real email |
| Error monitoring | Sentry (planned, TRD §10) | not yet added |
| Auth | Split by role: **Customer** (Phase 10.5) = email + password, verified once at signup by a one-time emailed code, login by email+password only thereafter; **Transporter Company** (Phase 10.5, restructured Phase 10.8) = phone + full_name + email + password collected together at signup, verified by a plain SMS code, then password-based login (`POST /auth/company/login`) thereafter — the OTP itself is a one-time signup-verification step, never asked again | **working** — Sanctum tokens, Redis-backed OTP/email-code, rate-limited |
| Admin login | Email + password (`users.password_hash`) | **working** — minimal, ahead of the full Admin tool (Phase 9); bootstrap the first admin with `php artisan admin:create <phone> <email> <password>` |

## Local development setup

**Docker isn't usable on this particular machine** — it's physical hardware without VT-x/AMD-V exposed to Windows, so WSL2 (and therefore Docker Desktop) can't start a VM. `docker-compose.yml` is still there and correct for any machine that *does* support virtualization (a teammate's machine, CI, staging), but local dev here runs Postgres and Redis natively instead. If you're on a machine where Docker works, skip to "Alternative: Docker" below.

### 1. PostgreSQL + PostGIS

Already installed on this machine (PostgreSQL 18.4, via the official EDB installer, with the matching [PostGIS Windows bundle](https://postgis.net/windows_downloads/) installed on top). On a fresh machine:

1. Download PostgreSQL from https://www.postgresql.org/download/windows/ and install it (note the superuser password you set).
2. Download the matching PostGIS bundle for your PostgreSQL major version from https://download.osgeo.org/postgis/windows/ (e.g. `pg18` → `postgis-bundle-pg18x64-setup-*.exe`) and run it.
3. Create the app's role, database, and enable the extension (run as the `postgres` superuser):
   ```sql
   CREATE ROLE cargo_motives LOGIN PASSWORD 'secret';
   CREATE DATABASE cargo_motives OWNER cargo_motives;
   \c cargo_motives
   CREATE EXTENSION IF NOT EXISTS postgis;
   ```
   This matches `backend/.env`'s `DB_USERNAME`/`DB_PASSWORD` already — no `.env` changes needed. The superuser password is never stored in the app config, only used for this one-time setup.

**Note on installers needing elevation:** on this machine, running the Postgres/PostGIS installers (and an MSI-based Redis installer we tried first) from an automated/non-interactive shell failed — Windows couldn't show the UAC consent prompt those installers need. Running them from a normal interactive **Administrator** PowerShell/terminal works fine.

### 2. Redis (portable, no installer)

An MSI-based Redis-compatible service (Memurai) consistently failed to install on this machine even from an elevated terminal — Windows Installer's own SYSTEM-level temp directory was inaccessible, which looks like a machine-specific restriction unrelated to normal admin rights. The workaround: a portable, dependency-free Redis build ([tporadowski/redis](https://github.com/tporadowski/redis/releases), an unofficial but widely-used Windows port) that's just an `.exe` — no installer, no service, no elevation needed.

Already set up in `.local-services/redis/` (gitignored — it's a downloaded third-party binary, not project source). To start it:

```powershell
powershell -File .local-services\start-redis.ps1
```

This is **not a Windows service** — it's a plain foreground process, so it needs to be started again after every reboot or terminal close. Leave that terminal open while working on the backend, or run it in its own background terminal/tab.

### 3. Backend

```bash
cd backend
php artisan migrate
php artisan serve
```

### 4. Verify

```bash
curl http://localhost:8000/api/health
```

Should return `{"status":"ok","checks":{"database":{"ok":true},"redis":{"ok":true}}}`. If either check is `false`, the error message names which dependency is unreachable and why — see `app/Http/Controllers/HealthController.php` for what each failure mode means.

### Alternative: Docker (on a machine that supports virtualization)

```bash
cp .env.example .env
docker compose up -d
docker compose exec app php artisan migrate
curl http://localhost:8000/api/health
```

This also brings up MinIO (S3-compatible storage) and Reverb, which the native setup above doesn't include yet — needed once file uploads (Phase 2+) or WebSockets (Phase 4+) are being tested locally, if this machine's Docker situation doesn't change before then.

### Mobile app

```bash
cd mobile
flutter pub get
flutter run          # or: flutter run -d chrome
```

## Phase status

Tracking the Implementation Plan document's 12 phases. Each phase is built, explained, and verified before the next starts.

- [x] **Phase 0 — Foundations**: Laravel + Flutter scaffolds, Docker Compose, vendor choices documented, `/api/health` endpoint, SMS driver abstraction. Verified end-to-end against a real (natively-installed) Postgres+PostGIS and Redis — not just against sqlite/mocks.
- [x] **Phase 1 — Auth (Customer & Company)**: shared phone/OTP flow (`App\Services\Auth\OtpService`, Redis-backed with attempt lockout + resend cooldown), phone number normalization, Sanctum tokens, rate limiting (`otp-request`/`otp-verify` limiters), Customer profile completion. Flutter: Phone Entry → OTP → Profile Setup screens, `ApiClient`/`AuthRepository`, session persistence. 29 backend tests + 13 Flutter tests passing; walked the full flow live in a browser against the real stack for both roles (Customer → Profile Setup → Customer Home; Transporter Company → Company Home placeholder). Found and fixed two real bugs along the way: a default Laravel guest-redirect that 500'd unauthenticated API requests lacking an `Accept: application/json` header, and an OTP lockout off-by-one.
- [x] **Phase 2 — Company Verification (manual review, anti-duplicate)**: two-section verification submission (`App\Http\Controllers\Company\CompanyVerificationController`), `CompanyDuplicateDetector` (a registration number/TIN/NIDA collision is flagged for Admin, never silently accepted or rejected — deliberately *not* a DB-level unique constraint, since that would make flagging impossible), private document storage with signed URLs (`DocumentStorage`), and a minimal Admin login + review API (list/approve/reject) ahead of the full Admin tool (Phase 9). Flutter: `CompanyHomeGate` routes a Transporter Company session to the verification form, a pending-review screen, or Company Home based on live status — checked fresh on every session, not just after login. 61 backend tests + 22 Flutter tests passing; walked all four states (submit → flagged-duplicate → admin reject → resubmit → admin approve → Company Home) live against the real stack, including a live approve/reject through the Admin API. Found and fixed three real bugs: two mass-assignment gaps where a field silently got dropped because it wasn't in a model's `$fillable` list (`owner_user_id`, then `password_hash` — the latter meant the admin-bootstrap command created unusable accounts, invisible to factory-based tests since factories bypass mass-assignment), and a `setState()` given a closure that returned a `Future` (an easy-to-miss Dart gotcha from an arrow function whose body was an assignment expression).
- [x] **Phase 3 — Trucks & Drivers**: truck registration with independent Admin verification (`App\Http\Controllers\Company\TruckController` — same create-or-resubmit-if-rejected shape as company verification), a driver roster with no verification workflow (drivers are never Admin-reviewed, PRD §5), a reusable `EnsureCompanyApproved` middleware (fleet management only opens up once the company itself is approved — Phase 4's bidding will need this exact check again), and a matching Admin truck-review API. Flutter: a real Company Home shell (bottom nav — Jobs/Fleet/Earnings/Profile, UI/UX Brief §3) with a working Fleet tab (Trucks/Drivers sub-tabs, add/resubmit forms with `file_picker`) and a minimal but real Profile tab (company name + logout). 83 backend tests + 37 Flutter tests passing; walked the full flow live — register two trucks, approve one/reject the other via the Admin API, add a driver, confirm the Fleet tab and Profile tab both render real data end-to-end in a browser.
  - Found and fixed a real bug affecting every phase's models so far, not just this one: Eloquent's `create()` doesn't reflect a migration's DB-level `->default(...)` values on the in-memory model it returns — a freshly-registered truck's `gps_status`/`current_status`/`is_active` were `null` in the live API response despite the DB row being correct. Fixed by declaring `protected $attributes` on `User`, `TransporterCompany`, `Truck`, and `Driver` to mirror their migrations' defaults.
  - Found and fixed a real Flutter/Dio bug before it ever shipped: Dio's default `ListFormat.multi` only brackets a list entry when the entry is a Map/List, not a `MultipartFile` — so uploading multiple truck photos would have silently sent them all under the same bare `photos` field name, and Laravel/PHP keeps only the *last* of several identically-named non-bracketed multipart parts. Fixed with `ListFormat.multiCompatible`; pinned with a regression test since it's the kind of thing a future Dio upgrade or an "innocent" refactor could silently reintroduce.
- [x] **Phase 4 — Job Posting & Bidding**: jobs/bids schema (Backend Schema §2.7) with PostGIS `geography(Point,4326)` pickup/dropoff columns (no Eloquent cast exists for these — written via raw `ST_SetSRID(ST_MakePoint(...))` through a small `GeoPoint` value object, read back via a `withCoordinates()` query scope using `ST_X`/`ST_Y`), a partial unique Postgres index enforcing "one pending bid per company per job" (`WHERE status = 'pending'`, not a plain unique constraint — a company can re-bid after withdrawing or being rejected), and Redis sorted-set rolling-window quotas (`RollingQuotaService`, `BidQuotaService`, `JobPostQuotaService`) for both customer job-posting (5/day standard, 10/day Featured) and company bidding (5/24h standard, 10/15h Featured) — deliberately not fixed-window `INCR`+`EXPIRE`, which lets a burst empty and refill a quota right at the window boundary. Bid acceptance (`BidController::accept`) runs in a DB transaction with `lockForUpdate()`, assigning the job and rejecting every other pending bid atomically. First real use of Reverb: `BidPlaced` broadcasts on a private `job.{jobId}` channel, authorized by a class-based `App\Broadcasting\JobChannel` (not an inline closure) so channel authorization is directly unit-testable without an HTTP round trip. Flutter: Customer Post-a-Job form + Job Detail screen with a live-updating bid list (Featured bids pinned first) via a hand-rolled minimal Pusher-protocol WebSocket client (`JobBidChannel`, using `web_socket_channel` — the obvious `pusher_channels_flutter` package turned out to hardcode Pusher's own cloud infrastructure host with no way to point it at a self-hosted Reverb server) and Accept Bid; Company Open/My Bids/Active job feeds (tabbed, mirroring the Fleet tab's Trucks/Drivers split) and a bid-placement form with live quota display. 132 backend tests + 55 Flutter tests passing. Walked the full flow live against the real stack (Postgres, Redis, a running Reverb server, and a queue worker — `BidPlaced` is a *queued* broadcast, so nothing reaches a socket without one): posted a job as a customer, subscribed to its private channel with a real WebSocket client, placed a bid as an approved company, and watched the `bid.placed` event arrive over the open socket in real time before accepting the bid and confirming the job transitioned to `assigned` with `agreed_price` set; also confirmed quota counters decrement correctly and a duplicate-pending-bid attempt is rejected with 422.
  - Found and fixed a real test-infrastructure gap, not just a Phase 4 edge case: PostGIS raw SQL (`::geography`, `ST_MakePoint`) cannot run against SQLite at all, and PostGIS is now core to the schema — so `phpunit.xml` was switched from an in-memory SQLite DB to a dedicated real Postgres database (`cargo_motives_test`), with a dedicated Redis logical DB (`REDIS_DB=2`) alongside it so quota tests can't leak state into (or read stale state from) dev Redis.
  - Found and fixed a subtle Redis bug during that same isolation work: `Redis::keys('prefix:*')` returns keys *with* Laravel's configured key prefix already applied, so passing those results straight to `Redis::del()` applies the prefix a second time and silently deletes nothing — quota state was leaking between test runs (and into dev Redis) until this was caught via `tinker`.
  - Found and fixed a broadcast-channel-authorization testability dead end: `NullBroadcaster::auth()` (active under `BROADCAST_CONNECTION=null` in tests) never invokes registered channel-authorization callbacks at all, and switching a test to the real Reverb driver hit deeper signing complexity. Refactored the inline channel closure into `App\Broadcasting\JobChannel` so its `join()` logic could be unit-tested directly, sidestepping the broadcaster entirely.
- [x] **Phase 5 — Job Assignment, Driver Link & Proof of Delivery**: `driver_links` (a single-use, no-login, short-lived token scoped to one job — TRD §7: "never a Sanctum token") and `proof_of_deliveries` tables (Backend Schema §2.5/§2.10). `JobAssignmentService` picks a truck + driver for a job the company has already won (separate from Phase 4's bid acceptance — a finer-grained, re-doable step: a truck breakdown mid-job can be reassigned), transactionally frees the previous truck, flips the new one to `on_job`, invalidates any still-active link for the job, and issues a fresh one, texting it via the existing `SmsGateway` abstraction (still `SMS_DRIVER=log`) — a send failure degrades silently (TRD §5.3), since the link is always returned to the caller to display/re-share regardless. The Driver Link itself is the app's first server-rendered page (`resources/views/driver-link/*`, plain Blade + inline CSS, no Vite/Tailwind build step) rather than a Flutter screen — the TRD specifies it as a "lightweight server-rendered mobile web page, no login, no app install," and a driver has no account for a Flutter screen to authenticate. It lets a driver optionally advance the job through `en_route_pickup`/`picked_up`/`in_transit` (forward-only, enforced against the canonical status order) before submitting proof of delivery (camera-first `<input capture="environment">`, photos + optional recipient name/notes) via `DocumentStorage`, atomically marking the job `delivered` and the link `used`. "Fans out instantly to the Customer, the Company, and Admin's record" (AppFlow §4) is satisfied by that data becoming visible on the next fetch of the job (`JobResource` now exposes `assigned_truck_registration`/`assigned_driver_name`/`proof_of_delivery`) — the same ordinary REST + refresh-on-open pattern already used for non-bid/non-GPS features (TRD §4), not a new push channel; no `notifications`/FCM infrastructure exists yet in this codebase; that's explicitly out of scope here (Backend Schema's `notifications` table isn't a deliverable of any of the 12 phases, and `activity_logs` writes are explicitly Phase 9's job). The customer's Confirm Receipt (`JobController::confirmDelivery`) moves the job to `completed` and frees the truck back to `idle`; it deliberately does **not** write a `commission_ledger` charge (that table doesn't exist until Phase 7) or open a dispute for "Report a Problem" (Phase 9's Admin tool owns disputes) — both explicitly out of this phase's scope. Flutter: Company `AssignJobScreen` (idle/approved truck + active driver pickers, a copyable driver-link confirmation), an Assignment section on `CompanyJobDetailScreen` (gated on a new `is_assigned_to_viewer` flag — a company can legitimately view a job it *lost* the bid on via "My Bids" after it moves past `open`, and must not see actionable assignment buttons on someone else's job); Customer `JobDetailScreen` gained a Proof of Delivery card (photo thumbnails, recipient/notes) with Confirm Receipt. 153 backend tests + 62 Flutter tests passing, Pint clean, `flutter analyze` clean. Verified live end to end against the real stack: assigned a truck+driver via the API (SMS logged, truck flipped to `on_job`), opened the real Driver Link page in a browser and advanced its status live, submitted proof of delivery with a real JPEG via a raw HTTP multipart request (CSRF token + session cookie fetched from the page first) — job flipped to `delivered`, link to `used`, photo downloadable via its signed URL — then confirmed delivery as the customer via the API, landing the job on `completed` with the truck freed back to `idle`.
  - Found and fixed a real mass-assignment gap — the same bug class as Phase 2/3, still recurring: `DriverLink`'s `#[Fillable(...)]` omitted `used_at`, so `$link->update(['status' => 'used', 'used_at' => now()])` silently dropped the timestamp; a test asserting `used_at` was set (not just `status`) caught it immediately.
  - Found and fixed a `nextDriverSettableStatus()` logic bug before it shipped: `array_search($currentStatus, $list, true) + 1` looks reasonable until `$currentStatus` isn't in `$list` at all (the job's real starting status, `'assigned'`) — PHP silently coerces `array_search`'s `false` return to `0` under arithmetic, so `false + 1` evaluates to `1`, which would have skipped straight to "picked_up" as a driver's very first status option instead of "en_route_pickup". Caught during self-review before running any tooling; fixed by handling the `'assigned'` starting case explicitly rather than relying on `false`'s arithmetic behavior.
  - The Blade view for an expired/used link (`show()`) and the corresponding `abort_unless(..., 410)` in the status-update/POD-submit actions render differently — the former shows a friendly branded page, the latter Laravel's generic error page for the same underlying condition. Left as a known, minor UX inconsistency (the request is still correctly rejected either way) rather than a blocking bug, given the phase's time budget; worth a custom `resources/views/errors/410.blade.php` in a later polish pass.
- [x] **Phase 6 — GPS: Wialon, end to end**: `gps_connections` (encrypted per-company provider credentials, Backend Schema §2.6) and `job_location_snapshots` (throttled route-replay points, §2.9) tables; `trucks.gps_connection_id` finally gets its real FK now that the table exists. A concrete `WialonGpsProvider` (`App\Services\Gps`, bound directly to a `GpsProvider` interface — no multi-provider manager/framework yet, matching the plan's "prove one provider end to end before touching a second") implements Wialon's real RPC-style API (`token/login` → session id → `core/search_items` with `flags=1025` to get unit list + last-known position in one call), each call a fresh stateless login/query/logout round trip rather than a persisted session. Connect GPS (`GpsConnectionController`) authorizes a company's token, returns every visible unit with a plate-text-matched suggested truck, and `import()` links confirmed matches (`gps_connection_id`/`gps_unit_id`/`gps_status='connected'` on the truck) — an unmatched or skipped vehicle stays "GPS Tracking Not Available," never an error. The async pipeline (TRD §5.2) is poll-based, not webhook — Wialon has no outbound webhook in this integration, so a scheduled `PollGpsPositionsJob` (`bootstrap/app.php`'s `withSchedule`, every minute) fetches each connected company's units and dispatches a `NormalizeGpsPositionJob` per truck with a position; the normalizer updates `trucks.last_known_*`, recovers `gps_status` from `signal_lost`, throttle-writes a `job_location_snapshots` row, and broadcasts `TruckLocationUpdated` on a **second**, new private channel (`job.{jobId}.location` — distinct from Phase 4's `job.{jobId}` bid channel, since GPS's audience is broader: both the customer *and* the assigned company watch the same truck, authorized by a new class-based `JobLocationChannel`). A company/truck failing to poll (bad token, provider outage) is marked `error` and skipped — never crashes the cycle for other companies (TRD §5.3). A separate scheduled `gps:check-signal-loss` command flips a trackable job's `gps_signal_status` from `ok` to `lost` once its truck's last position is older than a configurable threshold; recovery back to `ok` happens automatically the instant a fresh position arrives. `JobAssignmentService` decides a job's GPS-trackability once, at assignment time, from the truck's own `gps_status` — not re-derived on every read. Flutter: a `ConnectGpsScreen` (Wialon-only picker — Traccar/Tracksolid Pro shown but disabled, "coming soon") for the authorize → match-units → import flow, wired into `FleetScreen` as an app-bar action; a shared `GpsStatusCard` on both `JobDetailScreen` and `CompanyJobDetailScreen` rendering the three documented states (not available / live / signal lost) kept current by a second hand-rolled WebSocket client (`JobLocationChannel`, mirroring `JobBidChannel`'s pattern exactly). No real map is rendered — the same documented, already-established scope decision as Phase 4's plain lat/lng job-posting fields: no Google Maps API key is provisioned, so this is an honest live position readout (coordinates, compass heading, "updated Xs ago") rather than map tiles, while still fully exercising the real WebSocket data path. 183 backend tests + 70 Flutter tests passing, Pint clean, `flutter analyze` clean. Verified live end to end against the real stack: assigned a GPS-connected truck via the API (`gps_tracking_active`/`gps_signal_status` set correctly), dispatched a real position update through the actual Redis queue and watched `location.updated` arrive over a live WebSocket subscribed as the customer, confirmed the REST job endpoint surfaces the same live coordinates, backdated a truck's position and ran the signal-loss command live (`ok` → `lost`), confirmed automatic recovery (`lost` → `ok`) on the next position update, and confirmed Connect GPS's real HTTPS call to Wialon's actual API degrades gracefully (422 with a clear message) when the account doesn't exist/network fails, and that `PollGpsPositionsJob` marks a real failing connection `error` without affecting others.
  - Found and fixed a real, would-have-shipped bug: Carbon 3 changed `diffInMinutes()`/`diffInX()` to return a **signed** difference by default (Carbon 2 always returned positive) — `$recordedAt->diffInMinutes($lastSnapshot->recorded_at)` with `$recordedAt` always the newer timestamp came back negative, so `< $throttleMinutes` was true unconditionally and silently disabled the snapshot throttle forever after the very first write. This behavior's own test caught it (expected 2 snapshots after a 6-minute gap, got 1) — inspection alone would not have; fixed with `abs()` and a comment warning against trusting a `diffInX()` sign in this codebase again.
  - Found and fixed a second, related bug in the same method: comparing a fresh `Carbon` instance (from an Eloquent model read) against a raw `->value('recorded_at')` query-builder fetch (an uncast DB-driver string) silently broke the same throttle comparison — fixed by fetching through the model (`->first()`) so casts apply.
  - Found (but didn't chase further) a real local-machine constraint worth remembering for Phase 7's Selcom integration: this machine's PHP/cURL cannot verify `hst-api.wialon.com`'s TLS certificate chain (`SSL certificate ... self-signed certificate in certificate chain`) — a genuine outbound-HTTPS environment quirk, not a code bug. `WialonGpsProvider`'s exception handling converted it into a clean, correctly-surfaced `GpsProviderException` → 422 either way, so Connect GPS degrades exactly as designed even against this machine's real network conditions; any future phase making its own outbound HTTPS call (Selcom, Firebase) should expect to hit the same local CA-trust issue and not mistake it for a bug in that integration's code.
- [x] **Phase 7 — Commission Ledger & Mobile Money (Selcom)**: `payments` and `commission_ledger` tables (Backend Schema §2.13/§2.14 — `commission_ledger` uses the schema's literal singular table name, not Eloquent's pluralized default, so the model sets `$table` explicitly; append-only, `const UPDATED_AT = null`). A single `CommissionLedgerService` is the *only* place that ever writes a ledger row or mutates `transporter_companies.outstanding_balance`/`commission_standing` — `chargeForCompletedJob()` (fired from `JobController::confirmDelivery`) adds `agreed_price × commission_rate_default%` (a `platform_settings` value, seeded to 30 to match the TRD's fixed "30%," but Admin-adjustable later, not hardcoded) as a charge; `applyPayment()` (fired only from a confirmed webhook, never the initiate-payment response) subtracts a succeeded payment, clamped at zero. Both lock the company row first (`lockForUpdate`, the same pattern as Phase 4's bid acceptance) so a charge and a payment landing at nearly the same moment can't interleave into a wrong balance, and both recompute `commission_standing` fresh from the resulting balance against `commission_hold_threshold` (seeded to TZS 500,000 — no source number exists in the docs, picked as a working placeholder since the phase's own deliverable needs one) in either direction, so it can never drift. `BidController`'s on-hold bid-rejection (stubbed back in Phase 4) now has real balance changes to react to. A concrete `SelcomMobileMoneyGateway` (bound directly to a `MobileMoneyGateway` interface, same one-aggregator-no-framework reasoning as Phase 6's GPS provider) pushes a USSD-style mobile money charge and verifies inbound webhook signatures (HMAC-SHA256 over the raw request body) — its docblock is explicit that, unlike Wialon's well-established public API, its exact wire shape (endpoint path, signed-header names) is a best-effort model with no live Selcom account to verify it against (`SELCOM_*` env vars are unset), while the domain logic it feeds (ledger, idempotency, graceful degradation) is fully built and tested independent of that uncertainty. `SelcomWebhookController` is unauthenticated (no Sanctum guard — Selcom's server has no session) but never trusted without a valid signature, and is idempotent both ways: a retried `COMPLETED` delivery never double-credits, and a late/out-of-order `FAILED` delivery never downgrades an already-`succeeded` payment. Flutter: a real `EarningsScreen` (replacing Phase 3's placeholder) with the running balance, a Pay via Mobile Money bottom sheet (amount capped client-and-server-side at the current balance), and the ledger history; an On Hold banner shown across every Company Home tab (not just Earnings) when `commission_standing` is `on_hold`, fetched once at shell level per AppFlow §2.1's "status banners only when relevant." 206 backend tests + 76 Flutter tests passing, Pint clean, `flutter analyze` clean. Verified live end to end against the real stack: completed a real job and watched the exact 30% charge land in the ledger and balance, completed a second high-value job that crossed the threshold and confirmed bidding was actually rejected (422) for that company, confirmed a real HTTPS charge-initiation call to Selcom's sandbox degrades gracefully (a real connection-timeout failure recorded as a clean `failed` Payment row, not a crash), then self-signed a webhook payload and confirmed it credits the ledger, lifts the hold, is correctly rejected with a wrong signature, and does not double-credit on a retried delivery.
  - Found and fixed a real bug before it ever ran: `CommissionLedger`'s table is named `commission_ledger` (singular, matching the schema doc literally) but Eloquent's naming convention guesses `commission_ledgers` (pluralized) from the class name — the very first test against it failed with `relation "commission_ledgers" does not exist`. Fixed with an explicit `protected $table = 'commission_ledger';`. Every other new model this phase (and Phase 4-6's) happened to pluralize regularly and never hit this; worth checking explicitly whenever a schema-doc table name doesn't sound like a clean English plural.
  - Confirmed Phase 6's local-machine outbound-HTTPS note applies here too, just differently: the live charge-initiation call to Selcom's sandbox failed with a connection timeout (not the TLS chain error Wialon hit) — a different failure mode, same underlying "this machine's real network path to a third-party HTTPS API is unreliable" constraint, and the same graceful-degradation code handled it correctly either way.
- [x] **Phase 8 — Featured Tier & Messaging**: the Featured purchase flow (AppFlow §2.7/§3.6) reuses Phase 7's payment infrastructure exactly as planned — `MobileMoneyPaymentInitiator` was extracted from `CommissionController` (Phase 7's one call site) once a second real call site (`Company\FeaturedController`, `Customer\FeaturedController` — the app's first dedicated Customer-namespace controller) needed the identical "create a Payment, push the charge, record the outcome" sequence, and `SelcomWebhookController` now branches on `payment.purpose` (`commission_payment` → `CommissionLedgerService`, `featured_company`/`featured_customer` → a new `FeaturedTierService`) rather than assuming commission. `FeaturedTierService::activateFromPayment()` is the *only* place `is_featured`/`featured_until` are ever written, fired exclusively from a confirmed webhook — never the initiate-purchase response — matching Phase 7's "confirmation is always asynchronous" rule exactly. Everything Featured is supposed to unlock was actually already built and already reading `is_featured` since Phase 4 (`BidQuotaService`, `JobPostQuotaService`, the `is_priority` bid flag) — this phase is genuinely *only* the purchase flow plus three new Featured-gated endpoints: a plain-text preferred-routes filter on the Open Jobs feed (`CompanyJobController::open`, address-substring matching against `preferred_routes` — not a geo query, since routes are free-text origin/destination, not coordinates), a real PostGIS `ST_DWithin` proximity query for "find a return load" after a delivery (`CompanyJobController::returnLoadSuggestions`), and a fleet-map endpoint narrowing `TruckController::index`'s data to just GPS-connected trucks (`TruckController::map`) — the last two both gated with `abort_unless($company->is_featured, 403, '...')`. Messaging (`messages` table, Backend Schema §2.15) is deliberately plain REST + refresh-on-open — TRD §4 scopes WebSockets to exactly live bids and live GPS, and messaging is neither — served by one shared `MessageController` sitting outside both the Customer and Company route groups (unlike everything else in this app, which is role-scoped at the route level) since the same job's thread has to be reachable by both its participants; authorization and the "mark the other party's messages read" side effect both happen inside the controller itself. Flutter: `CompanyFeaturedScreen`/`CustomerFeaturedScreen` (pricing, purchase, and — company-only — a preferred-routes editor), a preferred-routes toggle on the Open tab (fetched once so a non-Featured company never sees a control that would silently do nothing), a `FleetMapScreen`, and a shared `MessagesScreen` reachable from both Job Detail screens once a company is assigned. No real map rendered for the fleet view — same already-established, already-documented scope decision as Phase 4/6 (no Google Maps API key). 229 backend tests + 90 Flutter tests passing, Pint clean, `flutter analyze` clean. Verified live end to end against the real stack: purchased Featured for both a company and a customer via a real (failing, since no Selcom sandbox account exists) HTTPS charge attempt followed by a self-signed webhook confirming it — watched `is_featured` flip, the company's bid quota jump from 5→10, a placed bid come back `is_priority: true`, and the customer's post quota jump from 5→10; verified the preferred-routes filter genuinely excludes a real non-matching job while including matching ones; ran the real PostGIS return-load query against live job data; confirmed the fleet map is 403 before Featured and returns real GPS-connected truck data after; and sent real messages both directions between a customer and an assigned company, confirming the read-receipt flips correctly on each side's next fetch (visible on the *next* request, not the one that triggered it — a deliberate design, not a bug, matching what the read-marking side effect is actually for).
  - Found (and immediately fixed) two `abort_unless(..., 403)` calls with no message argument — a real, if minor, API-quality gap caught only by looking at what a real client actually receives: with `APP_DEBUG=true` (normal for local dev), an empty-message abort renders Laravel's full debug stack trace as the JSON body instead of a clean `{"message": "..."}"`, technically correct (403 either way) but a poor error message for any real API consumer. Fixed both (`TruckController::map`, `CompanyJobController::returnLoadSuggestions`) to match `FeaturedController::updatePreferredRoutes`'s existing pattern, which already passed one. **Always pass a message to `abort_unless`/`abort` for anything a real client might hit** — an automated test asserting only the status code will never catch a missing one.
  - Two README Phase-status lines (Phase 7 and, before this fix, Phase 8) had been accidentally duplicated in earlier edits — cleaned up while writing this entry; worth a quick glance at the file's tail after any future phase-status edit.
- [x] **Phase 9 — Admin Tool**: exactly the eleven tools from PRD §10, built as a real Laravel Livewire web app (`livewire/livewire` installed fresh — the repo's Tailwind+Vite scaffold, unused since the Laravel installer left it in place, is finally put to real use here) under `/admin`, session-authenticated via Laravel's own `web` guard against the same `users` table Customers/Companies already use (`User::getAuthPassword()` already pointed at `password_hash` since Phase 1, so `Auth::attempt()` works against it unmodified) — restricted to `account_type='admin'` by the same `EnsureAccountType` middleware every other role-scoped route group already uses, just paired with `auth:web` instead of `auth:sanctum`. `bootstrap/app.php`'s `redirectGuestsTo` (previously a hard `null`, since this was a pure JSON API before now) is now path-aware: `/admin/*` guests land on a real login page, everything else keeps the old "no redirect, clean 401" behavior. The eleven tools map to eight route groups: company verification (reusing Phase 2's `CompanyDuplicateDetector` for the flagged-duplicate side-by-side comparison), truck verification, user/company search (one search box over name/phone/email or name/registration/TIN), jobs (list/filter + a detail page folding in bid list, assignment, and a real activity-log-backed timeline — PRD's three separate line items 4-6, per TRD §8's explicit "assignment visibility (part of job detail)"), commission/payment status (a read-only view over `TransporterCompany.outstanding_balance`/`commission_standing` and `CommissionLedger`/`Payment` rows — Phase 7's `CommissionLedgerService` remains the only writer), disputes (a **new** `disputes` table + `Dispute` model built from scratch this phase, plus a new customer-facing `JobController::reportProblem()` — the "Report a Problem" AppFlow alternative to `confirmDelivery()` that Phase 5's docblock had explicitly deferred to "Admin's Disputes tool, Phase 9" — reviewed against proof-of-delivery and GPS-snapshot-history evidence, resolved with a note), a single activity-log view, a live-GPS view (reusing the exact `Truck::where('gps_status', 'connected')` + `last_known_*` read pattern Phase 8's fleet map established, just company-wide and un-gated), and a stats dashboard (jobs posted/completed, active companies, commission collected — deliberately just those four numbers, per the PRD's own "a handful of numbers, not a reporting suite"). A twelfth, smaller piece per TRD §8 — "no separate settings/configuration UI... beyond a single basic form" — edits all eleven `platform_settings` keys at once, busting `PlatformSettings`' 60-second read cache on save so an edit takes effect on the next request. The **new** `activity_logs` table (also built from scratch this phase) is populated entirely through six Eloquent model observers (`App\Observers\*`, registered in `AppServiceProvider::boot()`) watching `Job`/`TransporterCompany`/`Truck`/`Bid`/`Payment`/`Dispute` for the state changes the schema doc's own examples name (`company_approved`, `job_status_changed`, `bid_placed`, `commission_hold_applied`, `dispute_resolved`, …) — a deliberate departure from threading an explicit logging call through every controller across Phases 1-8, which would have meant touching a dozen call sites with real regression risk; an observer watching `wasChanged()` on save gets every existing and future write path for free, with the actor resolved best-effort from `request()->user()` (correctly `null` — "a system event" — for queue-job or webhook-originated changes with no request behind them at all). 267 backend tests passing (233 prior + 34 new — 4 for `reportProblem()`, 30 for the admin app's auth/tools, largely `Livewire::test()` component tests rather than HTTP-layer tests, since a Livewire action never goes through routing), Pint clean.
  - Live-verified end to end in a real browser against the real stack, seeded with representative data (a pending company, a flagged-duplicate company with a real conflicting match, an on-hold company with commission history and a failed payment, GPS-connected and signal-lost trucks, an open job with competing bids, and a delivered job with a real dispute): logged in as a real seeded Admin, rejected the duplicate company and watched the rejection and its `activity_logs` row (actor correctly attributed to the logged-in Admin, not "System") appear live; approved a pending truck; searched by company name and by registration number; drilled into a job's full detail (route, assignment, bids, and — separately — its open dispute); resolved that dispute with a note and confirmed it dropped out of the Disputes queue's default "open" filter; reviewed one company's commission ledger and payment-attempt history; confirmed the GPS view splits connected vs. signal-lost trucks correctly; and saved a real settings change, confirming both the immediate on-page feedback and that `PlatformSettings`' cached read picked up the new value without waiting for the 60-second TTL.
  - Found (and fixed) a real bug live-testing that no automated test could have caught: `session()->flash('status', ...)` after a Livewire action (approve/reject/resolve/save) never actually appeared to the user, because a Livewire action is an AJAX partial update that re-renders only the component's own root element — it never re-renders the surrounding Blade layout, so a flash message read there (`layouts/admin.blade.php`'s original `@if (session('status'))` block) was silently dead on arrival. `Livewire::test()` couldn't catch this either, since it only inspects the component's own rendered HTML, which is exactly the part that *was* working — the layout wrapper it never touches is where the bug actually lived. Fixed by moving confirmation feedback onto a plain public Livewire property (`$statusMessage`) rendered inline in each component's own view instead of Laravel's session-flash mechanism, and removed the now-dead layout block entirely. **Any future Livewire action needing to confirm success to the user must render that feedback from the component's own state, never from a session flash the surrounding layout reads** — this is a structural fact about how Livewire's partial-update model works, not a one-off bug.
  - A second thing only live-testing surfaced (not a bug, but worth recording): this browser automation environment auto-dismisses native JS `confirm()` dialogs as `false`, which silently blocks any `wire:confirm`-gated action (`Approve`/`Resolve` buttons) from a simulated click — worked around during verification by calling the Livewire component's action directly via `component.$wire.<method>()` in the browser console instead of clicking through the dialog. `wire:confirm` itself is unaffected for a real user in a real browser; this is purely a constraint of this specific automated-testing setup, not an application defect — noted here so a future session doesn't waste time re-diagnosing it.
- [x] **Phase 10 — Hardening & Launch Prep**: not new features — a security/regression audit (TRD §7's exact checklist) followed by fixing every real gap it found, then localization and store-submission prep. **Security**: rate limiting and Sanctum/Driver-Link auth were both already correct (confirmed, no change); private-document/signed-URL storage was already correct too. Two real, financially-relevant gaps were found and fixed: (1) `SelcomWebhookController`'s idempotency check read `payment.status` *before* entering its transaction — a TOCTOU race where two genuinely concurrent webhook deliveries (Selcom does retry) could both pass the guard and double-credit the ledger; fixed by moving the check inside the transaction against a `lockForUpdate()`-locked row, so the second delivery blocks until the first commits and then correctly no-ops. (2) `AdminCompanyController::approve()` flipped a company to `approved` completely unconditionally — the migration's own comment claimed a duplicate re-check happened "defensively before approval," but no code actually did that; two companies sharing a `registration_number`/TIN/NIDA could both end up approved. Fixed with a new `CompanyVerificationService::approve()` (used by both the API controller and the Phase 9 Livewire component) that re-checks for an already-*approved* conflict first, plus a genuine DB-level backstop — a **new** partial unique index on `transporter_companies` scoped to `WHERE verification_status = 'approved'` — for the race the application-layer check alone can't fully close; a pending/flagged sibling still isn't itself a blocker, only an approved one is, so the flagging workflow's coexistence design is untouched. Also added `trustProxies()` (env-configurable, empty by default) and documented `SESSION_SECURE_COOKIE` for production, since TRD §7's "all traffic over HTTPS" can't be verified in local dev but silently breaks `$request->secure()`-dependent behavior behind an untrusted TLS-terminating load balancer. **Regression**: added the exact-threshold-boundary and already-on-hold-company edge cases `CommissionLedgerServiceTest` was missing; found and fixed a real gap in `CheckGpsSignalLoss` — its query could never match a truck whose GPS was marked connected at assignment but which then never sent a single real position at all (`last_known_at` stays `NULL` forever, and SQL comparisons against `NULL` are neither true nor false), leaving such a job showing a misleading "live" indicator forever instead of TRD §5.3's "GPS signal unavailable" state; fixed with a **new** `jobs.gps_tracking_started_at` column (set alongside `gps_tracking_active`/`gps_signal_status` in `JobAssignmentService`) giving that specific case something to measure elapsed time against. Also closed a real test-coverage asymmetry: the company-side job detail screen had no signal-lost widget test, unlike its customer-side counterpart showing the identical `GpsStatusCard`. **Localization**: built real `flutter gen-l10n` infrastructure from scratch (no prior `.arb`/codegen existed) — `LocaleController`/`LocaleStore`/`LocaleScope` make the app's locale genuinely changeable at runtime (previously hardcoded to `Locale('sw')` as an explicit Phase-0 placeholder), a **new** `POST /auth/profile/language` endpoint syncs the choice to `User.language_preference` (previously written once at signup and never read again by anything), and a shared `LanguageSwitcherTile` in both roles' Profile tabs lets a user actually change it — verified live in a real browser build that the whole Welcome → Phone Entry flow genuinely re-renders in Swahili. Fully localized this phase: the entire Auth vertical slice (Splash/Welcome/Phone Entry/OTP/Profile Setup) and both home shells' bottom-nav labels — a deliberately scoped, complete "vertical slice" rather than a shallow pass over all ~300 user-facing strings across the app's ~54 files (honestly noted as still English-only, for a future pass). **Launch prep**: fixed a real release-blocking bug — the Android manifest's `INTERNET` permission existed only in the debug/profile variants, never `main`, meaning a release APK/AAB as configured would have shipped with every network call silently failing; also fixed the Android app label and both `pubspec.yaml`'s and the web build's still-default "cargo_motives"/"A new Flutter project" placeholders. Generated a real (if programmatically-drawn, no image tool was available in this environment) app icon — a shipping-container mark in the app's own brand colors — wired into both platforms via `flutter_launcher_icons`, replacing the Flutter template default. Authored and hosted real Privacy Policy and Terms of Service pages (`/legal/privacy`, `/legal/terms`, plain Blade — same no-build-step pattern as the Driver Link) since both app stores require a hosted privacy URl at submission, and drafted full store-listing copy (`STORE_LISTING.md`) — description, keywords, permissions, and an honest list of what still needs a human (a real developer account, real screenshots, real signing credentials) rather than fabricating placeholders for those. 279 backend tests passing (267 prior + 12 new), 97 Flutter tests passing (91 prior + 6 new), Pint and `flutter analyze` both clean.
  - Live-verified in a real browser: the privacy policy page renders correctly at its real route; a real Flutter web build's Welcome and Phone Entry screens genuinely render in Swahili end to end (not just passing widget tests) after the locale-wiring rewrite.
- [x] **Phase 10.5 — Pre-Launch UX & Customer Auth Overhaul** (user-requested, ahead of Phase 11): five changes requested directly by the user after seeing the app end to end, ahead of launch. **Splash screen**: a real branded splash (blue background, "Cargo Motives" title) replacing the previous instant-redirect placeholder — a `Future.wait([sessionCheck, Future.delayed(900ms)])` guarantees it's visible for a minimum time even when the session check resolves instantly, and the title fades/scales in via `TweenAnimationBuilder`. **Default language + pre-login switcher**: `LocaleController.defaultLocale` flipped from `Locale('sw')` to `Locale('en')` (an explicit, user-directed departure from the original PRD's "Swahili default"), and a new `LanguageMenuButton` (device-only `PopupMenuButton`, no backend sync — distinct from the existing post-login `LanguageSwitcherTile`, which does sync) sits top-right on Welcome, Phone Entry, and OTP. **Customer auth rearchitected**: Customer signup is no longer phone+SMS-OTP + a later profile-setup step — it's now full_name/email/phone/password+confirmation (company name + logo optional, shown to companies bidding on that customer's jobs — user's explicit choice) verified by a **one-time emailed code** (`EmailOtpService` + `CustomerOtpMail`, reusing the existing `MAIL_MAILER=log` graceful-degradation pattern the same way `SMS_DRIVER=log` already worked, extending it to a genuine second real use), with login afterward by **email + password only** (user's explicit choice, not a fresh OTP every time) via a new `CustomerAuthController` (`register`/`verifyRegistration`/`login`). The registration form's data (including an already-bcrypt-hashed password — never plaintext, even transiently) is stashed in Redis under the email until the code is verified, at which point the real `User` row is created atomically — mirrors the existing phone+OTP flow's own "verify is the actual account-creation moment" design, and means an abandoned signup never leaves an orphaned row. The old Customer profile-setup step (`ProfileController::complete`, `ProfileSetupScreen`) is now fully deleted, not left dead. **Transporter Company Step 1 gains full_name/email**: per the user's explicit field list, `VerifyOtpRequest` now collects `full_name`/`email` alongside the phone+OTP code at account-creation time instead of deferring them (Step 2's company registration, already matching the user's requested field list closely, is unchanged). `RequestOtpRequest`/`VerifyOtpRequest` no longer accept `account_type=customer` at all — that flow now only exists for `transporter_company`. A Customer's optional `company_name`/`company_logo_url` reaches a bidding company through the same established `JobResource`/`whenLoaded()` eager-loading pattern already used elsewhere (`CompanyJobController`'s four job-listing methods now eager-load `customer`) rather than a new mechanism. **Interactive signup**: a new decorative `TruckRoadAnimation` (an infinitely-looping `CustomPainter` dashed road + a translating truck icon, `Curves.easeInOut`) on the Welcome screen — purely cosmetic, no real map/GPS data (a real map remains a documented, already-established scope gap — no Google Maps API key is provisioned; see the "what's needed to go live" list below). 289 backend tests passing (279 prior + 10 net new), 108 Flutter tests passing (97 prior + 11 net new), Pint and `flutter analyze` both clean. Verified live end to end against the real running stack (not just tests): Customer register → a real email sent via the log-mail driver (inspected the actual rendered `CustomerOtpMail` HTML and headers in `storage/logs/laravel.log`) → verify created a correct `User` row (normalized phone, `email_verified_at` set, optional company fields stored) → login succeeded with the right password and failed cleanly with the wrong one; a job posted by that customer correctly surfaced `customer_name`/`customer_company_name` to a company via the real `/api/company/jobs/open` endpoint; the Transporter Company Step 1 flow verified end to end too — a real OTP requested and read from the log, verified together with `full_name`/`email` into a correctly-populated `User` row, its Step 2 company-registration multipart submission (business license + rep ID + rep selfie, real files) accepted and landed as `verification_status: "pending"`, and confirmed present in the same DB-level query the Admin verification queue itself reads. Also confirmed live in a real compiled Flutter web build (keyboard-driven, since this session's browser-automation tooling could not reliably click into `TextField`s on this particular canvas-rendered build — see gotchas below): Welcome renders in English by default with a working top-right language icon, selecting Kiswahili via the menu re-renders the whole screen live, and the `TruckRoadAnimation` visibly animates.
  - **New Flutter testing gotcha**: an infinitely-repeating `AnimationController` (`..repeat()`, used by `TruckRoadAnimation`) makes `pumpAndSettle()` hang forever on any screen containing it — must use bounded `tester.pump(duration)` sequences instead. A second-order issue: running two screens that both touch this animation as separate `testWidgets` blocks in the same file leaked timer state across the test boundary (intermittent failures) — fixed by merging them into one continuous test using `tester.pageBack()` to navigate back within the same `pumpWidget` instance, rather than two independent ones. **Any future screen using a `..repeat()` animation must follow this same bounded-pump pattern in its tests.**
  - **New Flutter testing gotcha**: adding any widget that reads `LocaleScope.of(context)` (even indirectly, via `LanguageMenuButton` in an AppBar) to an existing screen breaks every existing test for that screen with a `'scope != null'` assertion failure inside `LocaleScope.of()` (not a vague widget-not-found error — that's the diagnostic signal to look for) unless the test's `MaterialApp`/`MaterialApp.router` is wrapped in a `LocaleScope(controller: LocaleController(...), child: ...)` ancestor.
  - **Environment/tooling note, not an app defect**: this session's Flutter web build renders via canvas with essentially no real accessibility tree exposed to the browser-automation tool (`read_page` returns only `generic [ref_1]`) — `find`/click-based form-filling was unreliable on `TextField`s specifically (though it worked fine for buttons/icons), while keyboard `Tab`/`Shift+Tab` and arrow-key+Enter navigation worked reliably as a fallback. Given that, this phase's live verification of the Customer/Transporter registration pipelines was done via direct `curl` calls against the real running backend rather than through the browser UI — recorded here so a future session doesn't waste time re-diagnosing the same tooling limitation.
  - **What's still needed to make this real for a genuine launch** (asked of, and provided by, the user as this phase's fifth item — see the "Vendor / technology decisions" table above, now updated with Mail gateway and the split Auth row): a Google Maps API key (Android + iOS + a Maps JavaScript key if web is ever served for real) to replace the plain-lat/lng "no map" scope gap that's existed since Phase 4/6/8; a real SMS provider (Beem Africa was the Phase-0-era placeholder guess) to replace `SMS_DRIVER=log` for the Transporter Company OTP flow now that it's the *only* remaining OTP-driven signup; and everything Phase 10's `STORE_LISTING.md` already flagged (a real developer account, screenshots, signing credentials) for actually publishing to the stores. The Customer email-OTP flow's mail gateway is now real (Gmail SMTP, see the vendor table) — no longer on this list.
- [x] **Phase 10.6 — Push Notifications & In-App Notification Center** (user-requested, ahead of Phase 11): Firebase Cloud Messaging (TRD §1/§12) plus the in-app notification list it feeds, covering every real event in AppFlow §6's Notification Trigger Map that a mobile-app action can actually fire (the Driver Link's own SMS step and Admin's in-app dispute-dashboard note are untouched — neither goes through this new system). **Backend**: two new tables built exactly to spec — `notifications` (Backend Schema §2.18, append-only apart from `read_at`/`sent_via_fcm`) and a new `device_tokens` table (not in the schema doc — same "new infrastructure for a documented feature with no existing home" justification as Phase 6's `gps_connections`) storing one row per (user, device install), upserted by token so a device changing hands between accounts correctly re-points to its new owner on that account's next login. A `PushGateway` interface with `log`/`firebase` drivers mirrors `SmsGateway`'s exact Manager pattern (`PUSH_DRIVER=log` writes the push to the log instead of sending — the same graceful-degradation default as `SMS_DRIVER=log`); the real `firebase` driver uses `kreait/firebase-php` (FCM's real HTTP v1 API needs a Google OAuth2 token exchanged from a service-account JWT — client-library territory, not something worth hand-rolling the way Selcom's simpler HMAC scheme was) and never throws, always returning a `PushSendResult` the same way `SmsSendResult` never throws. `NotificationService::send()` is the one place a `notifications` row is ever created; it always succeeds regardless of Firebase's state, then queues a `SendPushNotificationJob` (TRD's architecture diagram already lists "notifications" as queue-worker work, alongside SMS/GPS-normalization/payment-webhooks) that reads the user's device tokens, sends, and prunes any token FCM reports as invalid/unregistered. Every trigger reuses Phase 9's activity-log observer pattern rather than threading calls through a dozen controllers — `TransporterCompanyObserver`/`TruckObserver`/`BidObserver`/`JobObserver` (already watching the exact state transitions that matter) gained notification calls alongside their existing audit-log ones, plus a new notification-only `MessageObserver` (messages still aren't audit-logged, per Phase 8's own decision — this observer exists solely for the trigger map's "new message" row). Two existing bulk `->update()` call sites (`BidController::accept()`'s losing-bid rejection, `CheckGpsSignalLoss`) were converted to per-row saves, since a bulk query-builder update never fires Eloquent model events and would have silently skipped every observer-driven notification for those rows. REST surface: `GET /notifications` (paginated), `GET /notifications/unread-count`, `POST /notifications/{id}/read`, `POST /notifications/read-all`, `POST`/`DELETE /notifications/device-token` — all plain REST + refresh-on-open (TRD §4: notification lists were never one of the two WebSocket use cases), under `auth:sanctum` shared by both roles like `MessageController`. **Mobile**: `firebase_core`/`firebase_messaging` added; a `PushNotificationService` wraps all Firebase interaction behind a never-throws `initialize()`/`registerDeviceToken()` pair (real device delivery needs a real Firebase project — see the vendor table below — but the app must behave identically without one, same principle as the missing Google Maps key), called once in `main()` and once from each home shell's `initState()` (covers both a fresh login and a resumed session, since Splash only ever lands there with a valid one). A `NotificationRepository`/`NotificationsScreen`/`NotificationBellButton` trio gives both roles a bell icon with an unread badge on their home tab (AppFlow §3.1's own wording for Customer, extended here to Company Home too since the trigger map is full of company-directed events and the doc simply never wrote a bell-icon line for that role). 324 backend tests passing (289 prior + 35 new), 116 Flutter tests passing (108 prior + 8 new), Pint and `flutter analyze` both clean. Verified live end to end against the real running stack: approved a real company via `CompanyVerificationService`, watched a `notifications` row land, the queued `SendPushNotificationJob` actually run against the real Redis-backed queue worker, and the real `log` push driver write the exact push payload (title/body/tokens/data) to the log with a real registered device token; exercised every REST endpoint via real `curl` calls (list, unread-count, mark-read together with its count dropping, device-token register/re-register-under-a-different-account/delete); and loaded the real compiled Flutter web build in a browser, confirming `main()`'s Firebase initialization doesn't crash the app without a configured project (zero console errors), the bell icon renders with its badge, and tapping it opens a real Notifications screen that made real `GET /notifications`/`GET /notifications/unread-count` calls against the live backend.
  - Found and fixed two real bugs along the way, not just built the feature: (1) a null-safety gap in `JobObserver` — `$job->assignedCompany->owner` crashed on a job with an assigned truck but no assigned company (a real possible state a couple of existing test fixtures already built, not just a hypothetical), fixed with a small `notifyAssignedCompanyOwner()` helper that no-ops on a null relation. (2) A **third** recurrence of the exact "Flutter `setState(() => x = someAsyncCall())` returns a Future" bug this project first hit in Phase 2 — `NotificationsScreen._refresh()` was written with the same buggy arrow-body pattern, AND writing its own tests (which actually tap the retry-feeding actions, unlike the copy-source) surfaced that **the original `CustomerJobsTabState.refresh()` this pattern was copied from had carried the identical bug, undetected, since Phase 1** — its own test only ever checked the retry button was present, never tapped it. Fixed both with a block body, and strengthened the existing test to actually tap "Try again" and assert the retry succeeds, closing the coverage gap that let it hide for 10+ phases. **This is now a 3-for-3 recurrence of the same bug class** (joining `#[Fillable]` gaps and `NULL`-comparison sweeps on the project's running list of "patterns worth a proactive check on every new screen," not just a one-off fix each time) — a fourth instance also appeared inline this phase in the `Notification` model's `#[Fillable]` list (missing `read_at`, caught immediately by `NotificationControllerTest`'s mark-read test) — that's the fourth confirmed instance of the Fillable-gap class specifically.
  - **Follow-up (2026-09-07): a real Firebase project wired in, no longer just the `log` driver.** The user created a real Firebase project and provided a service-account JSON (backend) plus `google-services.json`/`GoogleService-Info.plist` (mobile, downloaded directly from the Firebase console rather than via `flutterfire configure`, which needs its own separate login this machine's `firebase` CLI didn't have — not required, since the downloaded files are sufficient on their own). The service-account key lives at `backend/storage/app/firebase/` (already gitignored by Laravel's own `storage/app/.gitignore`, confirmed — never at the repo root, where it was first dropped). `PUSH_DRIVER=firebase` is now the real local default. Live-verified against the real project: sending to a fake token returned Google's own "not a valid FCM registration token" — proof the OAuth2 handshake with the service account succeeded, not just that the code ran. That live test surfaced a genuine bug: `FirebasePushDriver` discarded `invalidTokens` entirely whenever *every* token in a send failed (only a partial multi-device failure ever reported them), meaning a user with exactly one dead token would never have it pruned — fixed by letting `PushSendResult::failure()` carry `invalidTokens` too, with a regression test pinning it (325 backend tests). Also fixed a real local-machine issue this surfaced: PHP had no CA bundle configured at all (`curl.cainfo`/`openssl.cafile` both unset), which was silently the same root cause behind the Wialon/Selcom TLS failures documented back in Phases 6/7 — installing curl's own `cacert.pem` and pointing both ini settings at it fixed outbound HTTPS system-wide, not just for Firebase. Wiring Android's `google-services.json` in via the Gradle plugin (`android/settings.gradle.kts`/`android/app/build.gradle.kts`) triggered this project's **first-ever native Android build attempt** (everything before this was web-only) and immediately hit a pre-existing Kotlin version mismatch (`google_maps_flutter_android`'s transitive Kotlin stdlib needed a newer compiler than the project's pinned `2.1.0`) — bumped to `2.2.20`, unrelated to Firebase but blocking any Android build regardless, and a real gap worth knowing about before this project's next native-build attempt. `flutter build apk --debug` now succeeds with the real Firebase config baked in — the strongest correctness signal available without a physical device or a working emulator (this machine's disk was too full to boot the one configured AVD; not pursued further per the user, real-device delivery remains the one thing not literally watched happening on a screen, though every layer feeding it has been proven).
- [x] **Phase 10.7 — Transporter Company Document Split** (user-requested, ahead of Phase 11): the user re-stated their original Transporter Company field list, making explicit a gap between it and Step 2's actual implementation — "Company registration certificate, TIN certificate, Other required transport/business documents" are three distinct document concepts, where the app had one generic `business_license` upload standing in for all three since Phase 2. Split `SubmitCompanyVerificationRequest`/`CompanyVerificationController` into `registration_certificate` (required), `tin_certificate` (required), and `other_documents` (optional, zero or more files) — no migration needed, since `transporter_companies.documents` was already a flexible JSONB column (Backend Schema §2.2); `CompanyResource`'s `documents` mapping now signs either a single key or, for `other_documents`, a list of them. Mobile: `CompanyVerificationScreen` gained three file pickers in place of the old single "Business license" one (two required, one optional and multi-select), `CompanyRepository` sends `other_documents` as a bracketed multipart array using `ListFormat.multiCompatible` (the exact Phase 3 `TruckRepository` fix, reused unchanged for the same underlying Dio behavior). 327 backend tests passing (325 prior + 2 new), 116 Flutter tests passing (unchanged — this screen's tests only ever covered the no-attachment validation path, not real file selection, so the rename didn't add new coverage surface). Verified live end to end: submitted a real company verification via a PHP `CURLFile` script (git-bash's curl mangles bracketed multipart field names — the same Phase 2/3 workaround) with two "other documents," confirmed all four resulting documents came back as working signed URLs, then approved the company through the real Admin panel.
  - Found and fixed a real, unrelated bug while touching this exact code: the Admin Livewire company-review page (`Show.php`/`show.blade.php`) rendered `$company->documents` **completely raw** — `TransporterCompany.documents` stores private `DocumentStorage` keys, never real URLs, so every document link on this page had always been broken (a bare storage path as an `<a href>`, not a signed URL) since Phase 9 first built it. Never caught before because no test asserts on the *contents* of a rendered document link (Livewire tests check state, not that a specific href resolves), and the manual Phase 9 live-verification walkthrough apparently never actually clicked one. Fixed by moving URL-signing into `Show::render()` (a `signedDocuments()` helper producing `{label, url}` pairs, correctly numbering `other_documents`' multiple entries) and confirmed live — the link now returns a real 200 with the actual file. **A page that displays a `DocumentStorage` key must sign it before it reaches the view; grep for any other raw `->documents`/`_url` access outside a Resource class if this pattern needs auditing further.**
- [x] **Phase 10.8 — Design Import: Foundation + Auth Flow Restyle** (user-requested, ahead of Phase 11): three pieces. **(1) Selfie dropped**: the user re-stated their Transporter Company field list a second time without a representative selfie — dropped entirely from Step 2 (not just made optional in validation), `transporter_companies.rep_selfie_url` made nullable via raw SQL (`Schema::table(...)->change()` needs `doctrine/dbal`, not worth adding as a dependency for one column), `CompanyResource` signs it only when present; existing rows with a selfie already on file keep it untouched. **(2) A confirmation step + real pending-review copy**: both signup screens now show a "Submit for review?" confirmation dialog (Cancel/Submit) after validation passes and before the actual API call, asking the user to confirm everything's correct — a plain `showDialog<bool>`/`Navigator.pop(bool)` await, the same shape already used twice elsewhere in this app (`FeaturedScreen`'s route dialog, `CompanyJobDetailScreen`'s driver-link dialog); the pending-review screen's copy was rewritten to match what the user actually asked for verbatim ("Your verification is under review. You'll get access to your dashboard as soon as it's approved.") — live-verified in a real browser, screenshot-confirmed. **(3) The big one — a Claude Design mockup import**: the user commissioned a 40-screen mobile-app design ("Cargo Motives App.dc.html", the "Industry" visual system — navy `#1D2D3D`/steel-blue `#416180`/light-blue `#94BCE3`, Barlow Condensed headings over Barlow body, 6–10px rounded components), delivered via a local folder export after the `DesignSync` MCP tool's live-sync path needed an interactive-session authorization this session couldn't complete and "Send to Claude Code Web" wasn't controllable from here either. Given the size (40 screens vs. this app's ~54 already-built files across 13 phases), scoped to: build the design-system **foundation**, then apply it fully to the **onboarding/auth flow** only — the other ~35 screens are follow-up work. Comparing the mockup against this session's own hard-won, explicitly-specified auth flows surfaced a real conflict (the mockup draws Transporter with a password field and Customer with phone+SMS-OTP) — resolved with the user directly before writing any code: **Customer stays exactly as built** (Phase 10.5's email+password+emailed-code, restyled only — reverting it was explicitly declined), **Transporter's flow is restructured for real** to match the mockup (password added, full_name/email move from the OTP-verify step to before it), and a required Terms & Conditions checkbox is added to both signup screens (client-side gate only, no backend field — nothing asked for server-side consent recording) while Google/Apple social-login buttons are omitted entirely (no backing endpoint, and shipping a non-functional button was explicitly ruled out over showing them disabled). **Design tokens**: Barlow/Barlow Condensed bundled as local font assets under `mobile/assets/fonts/` (fetched via the jsDelivr mirror of the `google/fonts` GitHub repo after GitHub's own raw-content host returned transient 503s) rather than the `google_fonts` package's runtime fetch — `AppTheme`'s own prior comment already ruled out a network font dependency for patchy TZ connections, and the mockup's choice doesn't override that constraint; `AppColors` gained `ctaBlue`/`ctaBluePressed`/`lightBlue`/`surfaceSubtle`/`infoTint`/`border`/`textPrimary`/`textLabel`/`textSecondary`/`textTertiary` alongside the existing (unchanged) status-dot colors and the amber money-accent — `primary` (navy) and the new `ctaBlue` (steel-blue) are deliberately two different tokens, since navy reads as brand ink/dark-surface color and steel-blue is the one actual interactive color, a distinction the old single `primary`-for-everything scheme didn't have. **Backend restructuring** (mirroring `CustomerAuthController`'s already-established pending-cache pattern exactly, not inventing a new shape): `RequestOtpRequest` gained `full_name`/`email`/`password`(+confirmation); `AuthController::requestOtp()` hashes the password immediately and stashes `{full_name, email, phone_number, password_hash}` in `Cache::put()` keyed by the normalized phone, same TTL as the code; `VerifyOtpRequest` dropped back to just `phone_number`/`account_type`/`code`; `verifyOtp()` now reads the pending cache entry and creates the `User` from it (an already-existing user just gets a fresh token, no cache lookup needed); a new `POST /auth/company/login` (`CompanyLoginRequest` + `AuthController::login()`) mirrors `CustomerAuthController::login()` exactly, with its own `company-login` rate limiter. Two bulk-update call sites needed no changes here (unlike Phase 10.6's observer-notification work) since this phase doesn't touch bid/job rows. Mobile: `AuthRepository` updated to match, a new `CompanyLoginScreen` (mirrors `CustomerLoginScreen` exactly) plus a `/company-login` route, `OtpScreenArgs` now carries the full pending registration (not just the phone number) so "resend" can re-request with identical data — mirroring `CustomerOtpScreen`/`CustomerRegistration`'s already-established shape. `WelcomeScreen` rewritten from two-direct-buttons to the mockup's tap-a-card-then-Continue pattern (dropping the Phase 11 truck-road animation in the process — the new layout has no room for it and the mockup doesn't feature one; noted here since it was itself a direct, explicit prior request). 335 backend tests passing (327 prior + 8 new), 123 Flutter tests passing (116 prior + 7 new), Pint and `flutter analyze` both clean.
  - Two real Flutter bugs caught only by live-verification, not by the test suite: (a) `WelcomeScreen`'s 36×36 logo-mark `Container` silently stretched to the full row width — the classic `CrossAxisAlignment.stretch` footgun (a `Column` with `stretch` still overrides an explicitly-sized child's own width/height in practice), fixed by wrapping it in `Align`, the exact same fix already used one line above it for the language-menu button; (b) the same `WelcomeScreen` rewrite added enough content (icon, headline, subtitle, two role cards, a button, a login link) to silently overflow a `Column` that was never wrapped in a `SingleChildScrollView` — the third recurrence of this exact bug class in this codebase (Phase 1's `PhoneEntryScreen`, Phase 11's `CustomerRegisterScreen`, now this), reinforcing that **every new auth/form-shaped screen needs `SingleChildScrollView` from the start**, not just the ones that have already been bitten. A third, test-only issue: adding the Terms checkbox pushed the bottom "Log in"/"Sign up" link below the default test viewport, so `tester.tap()` on it started missing (a hit-test warning, not a compile error) — fixed with `tester.ensureVisible(...)` first, the same pattern `company_verification_screen_test.dart` already used for its own submit button.
  - `DesignSync`/`/design-login` note for future sessions: this MCP tool needs a one-time interactive-terminal authorization (`/design-login` from a real `claude` CLI session, not this desktop-app Code-tab session) before `list_projects`/`get_project`/etc. will work here; until that's done once on this machine, the practical path for importing a Claude Design project is the user exporting/downloading its files directly (as happened here) rather than live-syncing.
  - Live-verified end to end against the real running stack (not just tests): via `curl`/tinker — requested an OTP with the new fields, confirmed the pending cache held a real bcrypt hash (never the plaintext `password123`), verified with the real code from the log, confirmed the created `User` row, then logged in with the password (succeeding) and with a wrong password (cleanly rejected, 422). Via a real compiled Flutter web build in the browser — walked the entire new flow live: Welcome's tap-select-then-Continue, the restyled Transporter Step 1 form (all four fields + Terms checkbox + the real validation-error path for an unchecked box), the code-only OTP screen, landing correctly on "Verify your company" after verification, then a second pass logging in via the new `CompanyLoginScreen` with the just-created password and landing on the same screen — confirming the whole restyled, restructured flow works for both a brand-new signup and a returning login, not just in isolation.
- [x] **Phase 10.9 — Design Import: Remaining Screens** (user-requested, "finish all pages," ahead of Phase 11): the follow-up to Phase 10.8's foundation-plus-auth-flow slice — every other screen the mockup covers that has real data behind it, restyled to the same Industry system. Mobile-only; no backend files touched this phase. **Tablet support** (raised by the user mid-session, not in the mockup): `MaterialApp.router`'s `builder` now wraps every screen in a `ConstrainedBox(maxWidth: 600)` centered in a `ColoredBox` — a single global change rather than per-screen work, has zero effect at real phone widths, and 600dp is a genuine Material window-size-class breakpoint, not an arbitrary number. **Both Home dashboards rebuilt** (Customer and Transporter) into the mockup's Activity-card + Active-shipment/job pattern, including a new `AuthRepository.me()` (backed by the already-existing `GET /auth/me`) so the greeting can show a real name, and client-side search over the customer's own already-fetched job list. **Customer's Shipment Details** rebuilt around a real distance/remaining-distance stat (haversine, `job_geo.dart`) and a status ladder derived live from the job's current status rather than a fabricated per-event history (this app has no such endpoint); gained a real "Report a problem" action next to Confirm Receipt, wired to the existing `JobController::reportProblem()` endpoint via a new `JobRepository.reportProblem()` the mobile side simply hadn't called yet. **Transporter's job board and job detail** restyled into the mockup's card/Submit-a-Bid/Active-Job patterns — deliberately does not show competitors' bid prices (`BidController::index()` is Customer-only, by design, to keep bidding sealed) or let a company assign a truck before winning a bid (assignment has only ever happened after acceptance); the `Job` model gained `customerName`/`customerCompanyName` (the backend already returned them via `CompanyJobController::show()`'s eager-loaded `customer` relation — the mobile model just wasn't reading the two keys). **Fleet's truck/driver rosters** gained a real stats bar (total/on-a-job/pending-verification, all counted client-side from the already-loaded list) and mockup-style chip cards — deliberately omits the mockup's driver star rating, license-expiry date, and live duty status ("Driving/Resting/Off duty"), none of which the `Driver` model has. **Transporter Earnings** kept as what it actually is — the commission a transporter owes the platform — restyled into a navy summary card rather than reinvented as the mockup's unrelated "gross revenue from customers" concept, which this app has no payout-tracking data model for. **Messages/Chat** restyled to the mockup's bubble shapes (also fixing a real color-semantics bug: the sender bubble was using the amber money-only `accent` token, switched to `ctaBlue`); the mockup's separate cross-job "Messages" inbox screen isn't built, since chat in this app is always scoped to one job and no endpoint aggregates threads across jobs. **Notifications** now groups into Today/Earlier from each row's real `createdAt` and maps the actual backend notification-type strings (enumerated straight from the Observer classes that create them — `new_message`, `new_bid`, `bid_accepted`, `gps_signal_lost`, `truck_approved`, `commission_hold_applied`, etc.) to distinct icons instead of one generic bell for everything. Both bottom nav bars got a `NavigationBarThemeData` (white background, steel-blue selected state, a top border now that Material3's default elevation shadow is off). **Both Profile tabs** gained a real header card (avatar initial, name/phone/email via `AuthRepository.me()`, a verification/Plus badge) and a bordered account-actions list — deliberately omits the mockup's shipment count, star rating, "years on platform," Saved addresses, and Payment history, none of which this app tracks for a Customer. **Post a Job** rebuilt from one long form into the mockup's 3-step wizard (Route / Cargo details / Pickup & notes) with a real computed distance and cargo-type/container-size chips that fill the same still-editable underlying fields — only 3 steps, not the mockup's 4, since `JobSubmission` has no budget field (a job's price only exists once a bid is accepted, so the mockup's "Set your budget" step has nothing real to show). **Both Cargo Motives Plus screens** got the mockup's dark-gold premium hero treatment; Customer's benefit list stays to the one real perk (`JobPostQuotaService`'s featured-vs-standard daily quota) while Company's five existing benefit rows (all already real — bid quota, bid priority, fleet map, route filter, return-load suggestions) just changed visual treatment. 129 Flutter tests passing (123 prior + 6 net new — two of which are new coverage on Post a Job's wizard navigation), `flutter analyze` clean; backend untouched, so its 335 tests are unaffected. **Deliberately not built**, each for a real (not scope-fatigue) reason: **Booking confirmation** — the mockup adds a customer-facing "service fee (2%)," but this app's commission model charges the transporter, never the customer, and accepting a bid already works as a direct one-tap action with no intermediate screen; adding one would mean fabricating a charge. **Settings** — almost the entire mockup screen (notification-category toggles, dark mode, 2FA/active-sessions, currency/distance-unit preferences, delete account) has no backend support at all; the only real setting (language) already lives in each Profile tab. **Help & support** — the FAQ content, support phone number/hours, and "your tickets" list are all either placeholder copy or backed by a `Dispute` model that has no listing endpoint (only creation, via Report a Problem). **Truck details** — the mockup's per-truck trip-count/on-time-rate/driver-tenure stats don't exist in this backend; the real equivalent data (company name, verified checkmark, truck count, rating, GPS availability, price, Accept) already renders inline on `_BidCard` in Shipment Details, so a separate drill-down screen would either duplicate that thinly or fabricate the rest. **Live GPS tracking / Fleet map** — both need a provisioned Google Maps API key, a documented, already-existing scope gap from earlier phases, unchanged here.
  - A rejected Bash tool call can still partially execute before the rejection takes effect: a bulk `sed` loop over 17 files was reported as rejected mid-session, but `flutter analyze` afterward showed roughly the first dozen files had actually been touched (missing only their new imports) while the last few in the array were never reached — the loop was interrupted mid-iteration, not before it started. Recovered by checking each file's actual on-disk state (`grep -c` for the substituted tokens and the new import) before deciding whether to add an import or redo the substitution, rather than trusting the "rejected" status at face value.
  - A recurring Flutter test gotcha, hit repeatedly this phase on every screen whose layout grew taller than the default 800×600 test surface: `ListView`/`SliverList` only materializes Elements within the viewport plus a ~250px cache extent, so `find.text()` on content further down returns zero matches — not because the widget isn't there, but because it was never built. Fixed with `tester.scrollUntilVisible(finder, delta, scrollable: ...)` before asserting, and `tester.ensureVisible(finder)` (not just `scrollUntilVisible`, which only guarantees the element exists, not that it's actually on-screen for a hit-test) immediately before any `tester.tap()` on such content — a `scrollUntilVisible`-only fix still produced an off-screen tap warning and a silently-no-op tap in one case (`company_job_detail_screen_test.dart`'s Confirm Receipt button) until `ensureVisible` was added right before the tap.
  - `IntrinsicHeight` is required around a `Row` whose child `Column` contains an `Expanded` (used for the vertical connector line between timeline dots) — a bare `Row` gives its children loose, not tight, cross-axis constraints, so the inner `Expanded` has no bounded height to expand into and throws a layout error on every frame, which manifested as `pumpAndSettle` timing out (thousands of caught-and-rethrown exceptions) rather than a single clear error message.
  - This project's Dart files are hand-formatted wider than `dart format`'s 80-column default (existing files run up to ~190 columns) — running plain `dart format` on a new/edited file reflows it to 80 columns and roughly doubles its line count, visibly inconsistent with the rest of the codebase. `dart format --line-length=140` matches the established density; used for every file touched this phase instead of the bare command.
  - Live-verified via the full Flutter widget-test suite (129 passing, including real interaction paths: the Post-a-Job wizard's step-by-step fill-Continue-fill-Continue-submit flow, a bid Accept button tap, a Report-a-problem dialog submit, a Confirm-Receipt tap) rather than a manual browser walkthrough of all ~20 restyled screens — given the volume of screens touched in one phase, exhaustive per-screen browser verification (as earlier phases did) was judged impractical; spot-checking was limited to reading rendered widget trees and trusting the same test suite that already caught two real layout regressions (the `IntrinsicHeight` timeout and the off-screen-tap issues above) during this phase's own development.

- [x] **Phase 10.10 — Design Import: The Remaining Pages, Real Where Possible** (user-requested, explicitly overriding Phase 10.9's "don't build it if it's not real" default for this slice): the user asked directly for every remaining mockup page to exist in the app now, even where no backend supports it yet, rather than waiting for that backend work — "finish the UI first, ask me requirements after." Confirmed three defaults via AskUserQuestion before building: mock/unbacked controls are **locally stateful** (flip and persist on-device, not fake-static), map screens get a **styled placeholder box** sized where a real `GoogleMap` will go, and **Booking Confirmation wires in for real** (accepting a bid now genuinely routes through it, not just a reachable extra screen). New `LocalPrefs` (a thin `shared_preferences` wrapper) backs every on-device-only value from here on. **Booking Confirmation**: a real screen between tapping Accept and the actual `bidRepository.accept()` call — route/transporter/price recap, no fabricated service-fee line (this app's commission charges the transporter, never the customer). **Shipment Posted**: `PostJobScreen` now push-replaces itself with a real confirmation screen (tracking ID + copy, route, cargo, "waiting for offers") instead of silently popping — the test harness wrapping it in a `GoRouter` route had to be swapped for a plain `Navigator`, since a page-based GoRouter route rejects the imperative `pushReplacement` production code actually needed. **Live GPS Tracking** (new, full-screen) and a rebuilt **Fleet Map** share a new `map_placeholder.dart` (grid background, pulsing marker, floating icon buttons) — every number on both (distance remaining, progress, on-a-job/available counts) is real, computed from the same haversine/`Job`/`Truck` data already used elsewhere, only the map tile background itself is a placeholder. **Truck/Transporter Details** (new): tapping a bid card now opens a fuller real view of that transporter (rating, rating count, verified truck count, GPS availability, the offer's route/distance/price) — skips the mockup's per-truck trip count and on-time percentage, which this app tracks nowhere, using the real rating/rating-count as the actual trust signal instead. **Settings** (both roles, new) and a shared **How It Works** guide (real, accurate 4-step explanation of the app's actual flow) and **Help & Support** (new — real FAQ answers, an honest empty "Your tickets" state rather than fabricated ticket history, since disputes have no listing endpoint) and **Change Password** (new — real form/validation, but says plainly there's no backend for this yet rather than pretending to save) round out the Profile-linked pages. **Payment History** (new) is the one screen built as clearly-illustrative sample data throughout — this app has no payment-ledger feature at all (customers pay transporters directly, outside the app) — except its default-payment-method row, which is real on-device state. **Saved Addresses** (new) is genuinely real CRUD against on-device storage, not a mockup facsimile, since there's simply no backend address-book endpoint yet to be a facade over. Both Profile tabs' account lists were reorganized to link everything above. 129 Flutter tests passing (unchanged count — this phase's new screens are mostly additive UI without dedicated new test files, matching the lighter verification bar the user's "finish the UI first" framing called for), `flutter analyze` clean; backend untouched. Added `url_launcher` (Terms/Privacy now open the real server-rendered `/legal/terms`/`/legal/privacy` pages) alongside `shared_preferences`.
  - **A real, unrelated bug surfaced by simply adding rows to an existing list**: `_AccountList`'s row `Container` (in both Profile tabs) set `color:` and `decoration:` directly on the same `Container` — Flutter's own assertion forbids this combination, but it had silently never fired because the one `highlighted: true` row (Cargo Motives Plus) always happened to be the *last* row in a short list, where the sibling `decoration` was `null`. Adding Settings/Help rows around it made Plus no longer last, decoration became non-null alongside the still-non-null highlight color, and the assertion fired immediately — caught by the existing `company_home_gate_test.dart`/`customer` test suite, not a new test written for this phase. Fixed by folding both into one `BoxDecoration`. **A conditional `color`/`decoration` pair on the same `Container` that has "worked so far" is not evidence it's correct — check whether the condition that's kept them mutually exclusive is coincidental (like list position) before trusting it as future rows get added.**
  - `Navigator.pushReplacement(route, result: someValue)` completes the **replaced** route's own popped-Future immediately with `result`, not when the new route eventually pops — this is what let `ShipmentPostedScreen` show on top of the navigation stack while still synchronously telling `CustomerHomeShell`'s `await Navigator.push<bool>(PostJobScreen())` that a job was posted (triggering its refresh) right away, without needing the confirmation screen to also carry that signal forward when it itself is later dismissed.
  - Confirmed a second time (see Phase 10.9's note on the exact same subject) that `dart format --line-length=140` — not the bare command — is this repo's real formatter setting; every file in this phase was formatted that way from the start rather than caught after the fact.

- [x] **Phase 10.11 — Messages Inbox** (continuation of Phase 10.10's mockup-completeness pass): cross-checking the mockup's own screen index against the app turned up one real gap — the mockup's top-level "Messages" screen (a list of conversations) had no equivalent; only the per-job chat thread (`MessagesScreen`, opened from inside a specific job) existed. Rather than fabricate a conversation list, new `MessagesInboxScreen` composes one entirely from real, already-existing endpoints: it takes the caller's job list (Customer's `JobRepository.list()`, Company's `CompanyJobRepository.active()`), keeps jobs with a real counterparty (`status` not `open`/`cancelled`), and fetches each one's real latest message, sorting the most recently active conversation first — no dedicated backend inbox endpoint needed or added. Company rows additionally call `CompanyJobRepository.show()` per job since `customer_name`/`customer_company_name` only come back on a single-job fetch, not the list. Wired in as a new chat-bubble icon next to the existing notification bell on both home tabs' headers, and next to Company Jobs' own bell. 132 Flutter tests passing (3 new, for the inbox's filtering/empty/error states), `flutter analyze` clean; backend untouched.

- [x] **Phase 10.12 — Beem Africa SMS Driver**: implements the `SmsManager::createBeemDriver()` stub that had sat waiting since Phase 0/1 ("throw until Beem Africa credentials are available"). New `App\Services\Sms\Drivers\BeemSmsDriver` calls Beem's real v1 API (`POST https://apisms.beem.africa/v1/send`, HTTP Basic Auth with the API key/secret, a JSON body naming the sender ID and recipients) — modeled on Beem's public documentation with the same honest caveat `SelcomMobileMoneyGateway` carries: no live Beem account exists in this environment to confirm the exact wire shape against, so double-check field/endpoint names against a real Beem dashboard before this ever carries production traffic. Strips the leading `+` from `PhoneNumberNormalizer`'s canonical `+255...` format before sending, since Beem expects a bare `255...` destination. Never throws — every failure (unreachable gateway, non-2xx/non-JSON response, a `successful: false` decline) returns `SmsSendResult::failure()`, the same graceful-degradation contract every driver honors, so an SMS outage still leaves OTP re-sendable and a Driver Link re-shareable. **No call-site changes were needed anywhere** — `OtpService` and `JobAssignmentService` (the Driver Link sender) already depended only on the `SmsGateway` interface; going live is purely a config change (`SMS_DRIVER=beem` + `BEEM_API_KEY`/`BEEM_SECRET_KEY` + an actually-Beem-approved `SMS_SENDER_ID`). Flagged and fixed in passing: the existing `SMS_SENDER_ID="CargoMotives"` placeholder is 12 characters, one over the 11-character cap on alphanumeric GSM sender IDs — `.env.example` now calls this out explicitly rather than letting it silently fail once someone actually registers "CargoMotives" with Beem. 8 new tests (`BeemSmsDriverTest`, `SmsManagerTest`) against faked HTTP responses, matching `SelcomMobileMoneyGatewayTest`'s own pattern; full backend suite 343/343 passing, `./vendor/bin/pint --dirty` clean.

- [x] **Phase 10.13 — Remove commission/escrow; Plus subscriptions become the only revenue path** (user-directed business-model pivot): the platform no longer takes a commission or holds any balance from a transporter company — the earlier "30% commission, on-hold above a threshold" model (Phases 7/9) is fully removed, not just hidden. **Backend**: a new migration drops the `commission_ledger` table and `transporter_companies.outstanding_balance`/`commission_standing`; deleted `CommissionLedgerService`, `CommissionLedger`, `Company\CommissionController`, and the Admin Commission Livewire pages/routes/sidebar link; `BidController::store()` no longer gates on commission standing (bidding is never blocked by a balance again); `JobController::confirmDelivery()` no longer charges anything; `SelcomWebhookController` only ever activates Featured now; Admin's dashboard stat swapped from "Commission collected" to a real **Plus subscribers** count (`User`/`TransporterCompany` rows with `is_featured`). The `payments.purpose` Postgres enum still technically allows the now-unused `commission_payment` value (altering/shrinking a live Postgres enum column was judged not worth the risk for an inert, never-written value — flagging this simplification explicitly). **Mobile**: deleted `CommissionRepository`/`EarningsScreen`; the "Account on hold" banner and Earnings quick-actions are gone from both the Company shell and dashboard. **Docs**: rewrote the README's own top-line business-model sentence and `legal/terms.blade.php` §4 ("Commission and payment" → "Payment" — now states plainly that Cargo Motives never collects or holds job payment, and that Plus is the paid product). Folded the Company bottom-nav restructure (Phase 10.14, see below) into this same pass rather than leaving an intermediate broken nav state, since removing the Earnings tab forced that decision anyway. 326 backend tests (17 deleted — the commission-specific suites — plus several adjusted rather than removed, e.g. `SelcomWebhookControllerTest` now exercises Featured activation instead of ledger credit; some other tests trimmed the now-fictional `commission_rate_default` field from `Admin\Settings\Edit`'s form checks), 125 Flutter tests, `./vendor/bin/pint --dirty` and `flutter analyze` both clean.

- [x] **Phase 10.14 — Footer restructure to match the mockup** (built alongside 10.13 — see that entry's note): the mockup's actual footer, read directly from its `data-go`/`showNav`/`showTNav` markup, is **Customer** = Home / Shipments / Messages / Profile and **Company** = Dashboard / Find Jobs / Messages / Profile — 4 tabs each, not the app's previous Jobs/Post/Profile (3) and Jobs/Fleet/Earnings/Profile (4). `CustomerHomeShell` and `CompanyHomeShell` were rebuilt around the new tab sets: Shipments (`CustomerShipmentsScreen`) is now a persistent tab instead of a "See all" link; **Messages is a real bottom-nav tab for both roles**, wrapping last session's `MessagesInboxScreen` directly rather than a standalone icon button (the redundant chat-bubble icons added next to the notification bell in Phase 10.11 were removed from `customer_jobs_tab.dart`, `company_home_tab.dart`, and `company_jobs_screen.dart` now that there's a real tab). "Post a shipment" and "Manage fleet" are no longer nav destinations — both were already real quick-actions from the Home/Dashboard tab, so nothing was lost, they're just reached the mockup's way now (Fleet opens via `Navigator.push`, not a tab switch). New l10n keys `navHome`/`navShipments`/`navMessages`/`navDashboard`/`navFindJobs` replace the now-unused `navJobs`/`navPost`/`navFleet`/`navEarnings` in both `app_en.arb` and `app_sw.arb`. This also resolves the "I can't find other pages" report — Shipments and Messages simply weren't reachable as persistent destinations before.

- [ ] Phase 11 — Launch & Learn
